/*-------------------------------------------------------------------------
 *
 * guc_tables.inc.c
 *    GUC tables
 *
 * Portions Copyright (c) 1996-2026, PostgreSQL Global Development Group
 * Portions Copyright (c) 1994, Regents of the University of California
 *
 * NOTES
 *  ******************************
 *  *** DO NOT EDIT THIS FILE! ***
 *  ******************************
 *
 *  It has been GENERATED by src/backend/utils/misc/gen_guc_tables.pl
 *
 *-------------------------------------------------------------------------
 */


struct config_generic ConfigureNames[] =
{
	{
		.name = "allow_alter_system",
		.context = PGC_SIGHUP,
		.group = COMPAT_OPTIONS_OTHER,
		/* translator: GUC parameter "allow_alter_system" short description */
		.short_desc = gettext_noop("Allows running the ALTER SYSTEM command."),
		/* translator: GUC parameter "allow_alter_system" long description */
		.long_desc = gettext_noop("Can be set to off for environments where global configuration changes should be made using a different method."),
		.flags = GUC_DISALLOW_IN_AUTO_FILE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &AllowAlterSystem,
			.boot_val = true,
		},
	},

	{
		.name = "allow_in_place_tablespaces",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "allow_in_place_tablespaces" short description */
		.short_desc = gettext_noop("Allows tablespaces directly inside pg_tblspc, for testing."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &allow_in_place_tablespaces,
			.boot_val = false,
		},
	},

	{
		.name = "allow_system_table_mods",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "allow_system_table_mods" short description */
		.short_desc = gettext_noop("Allows modifications of the structure of system tables."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &allowSystemTableMods,
			.boot_val = false,
		},
	},

	{
		.name = "application_name",
		.context = PGC_USERSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "application_name" short description */
		.short_desc = gettext_noop("Sets the application name to be reported in statistics and logs."),
		.flags = GUC_IS_NAME | GUC_REPORT | GUC_NOT_IN_SAMPLE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &application_name,
			.boot_val = "",
			.check_hook = check_application_name,
			.assign_hook = assign_application_name,
		},
	},

	{
		.name = "archive_cleanup_command",
		.context = PGC_SIGHUP,
		.group = WAL_ARCHIVE_RECOVERY,
		/* translator: GUC parameter "archive_cleanup_command" short description */
		.short_desc = gettext_noop("Sets the shell command that will be executed at every restart point."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &archiveCleanupCommand,
			.boot_val = "",
		},
	},

	{
		.name = "archive_command",
		.context = PGC_SIGHUP,
		.group = WAL_ARCHIVING,
		/* translator: GUC parameter "archive_command" short description */
		.short_desc = gettext_noop("Sets the shell command that will be called to archive a WAL file."),
		/* translator: GUC parameter "archive_command" long description */
		.long_desc = gettext_noop("An empty string means use \"archive_library\"."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &XLogArchiveCommand,
			.boot_val = "",
			.show_hook = show_archive_command,
		},
	},

	{
		.name = "archive_library",
		.context = PGC_SIGHUP,
		.group = WAL_ARCHIVING,
		/* translator: GUC parameter "archive_library" short description */
		.short_desc = gettext_noop("Sets the library that will be called to archive a WAL file."),
		/* translator: GUC parameter "archive_library" long description */
		.long_desc = gettext_noop("An empty string means use \"archive_command\"."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &XLogArchiveLibrary,
			.boot_val = "",
		},
	},

	{
		.name = "archive_mode",
		.context = PGC_POSTMASTER,
		.group = WAL_ARCHIVING,
		/* translator: GUC parameter "archive_mode" short description */
		.short_desc = gettext_noop("Allows archiving of WAL files using \"archive_command\"."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &XLogArchiveMode,
			.boot_val = ARCHIVE_MODE_OFF,
			.options = archive_mode_options,
		},
	},

	{
		.name = "archive_timeout",
		.context = PGC_SIGHUP,
		.group = WAL_ARCHIVING,
		/* translator: GUC parameter "archive_timeout" short description */
		.short_desc = gettext_noop("Sets the amount of time to wait before forcing a switch to the next WAL file."),
		/* translator: GUC parameter "archive_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &XLogArchiveTimeout,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX / 2,
		},
	},

	{
		.name = "array_nulls",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "array_nulls" short description */
		.short_desc = gettext_noop("Enables input of NULL elements in arrays."),
		/* translator: GUC parameter "array_nulls" long description */
		.long_desc = gettext_noop("When turned on, unquoted NULL in an array input value means a null value; otherwise it is taken literally."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Array_nulls,
			.boot_val = true,
		},
	},

	{
		.name = "authentication_timeout",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "authentication_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum allowed time to complete client authentication."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &AuthenticationTimeout,
			.boot_val = 60,
			.min = 1,
			.max = 600,
		},
	},

	{
		.name = "autovacuum",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum" short description */
		.short_desc = gettext_noop("Starts the autovacuum subprocess."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &autovacuum_start_daemon,
			.boot_val = true,
		},
	},

	{
		.name = "autovacuum_analyze_scale_factor",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_analyze_scale_factor" short description */
		.short_desc = gettext_noop("Number of tuple inserts, updates, or deletes prior to analyze as a fraction of reltuples."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_anl_scale,
			.boot_val = 0.1,
			.min = 0.0,
			.max = 100.0,
		},
	},

	{
		.name = "autovacuum_analyze_score_weight",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_analyze_score_weight" short description */
		.short_desc = gettext_noop("Scaling factor of analyze score for autovacuum prioritization."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_analyze_score_weight,
			.boot_val = 1.0,
			.min = 0.0,
			.max = 10.0,
		},
	},

	{
		.name = "autovacuum_analyze_threshold",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_analyze_threshold" short description */
		.short_desc = gettext_noop("Minimum number of tuple inserts, updates, or deletes prior to analyze."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_anl_thresh,
			.boot_val = 50,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "autovacuum_freeze_max_age",
		.context = PGC_POSTMASTER,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_freeze_max_age" short description */
		.short_desc = gettext_noop("Age at which to autovacuum a table to prevent transaction ID wraparound."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_freeze_max_age,
			.boot_val = 200000000,
			.min = 100000,
			.max = 2000000000,
		},
	},

	{
		.name = "autovacuum_freeze_score_weight",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_freeze_score_weight" short description */
		.short_desc = gettext_noop("Scaling factor of freeze score for autovacuum prioritization."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_freeze_score_weight,
			.boot_val = 1.0,
			.min = 0.0,
			.max = 10.0,
		},
	},

	{
		.name = "autovacuum_max_parallel_workers",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_max_parallel_workers" short description */
		.short_desc = gettext_noop("Maximum number of parallel workers that can be used by a single autovacuum worker."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_max_parallel_workers,
			.boot_val = 0,
			.min = 0,
			.max = MAX_PARALLEL_WORKER_LIMIT,
		},
	},

	{
		.name = "autovacuum_max_workers",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_max_workers" short description */
		.short_desc = gettext_noop("Sets the maximum number of simultaneously running autovacuum worker processes."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_max_workers,
			.boot_val = 3,
			.min = 1,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "autovacuum_multixact_freeze_max_age",
		.context = PGC_POSTMASTER,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_multixact_freeze_max_age" short description */
		.short_desc = gettext_noop("Multixact age at which to autovacuum a table to prevent multixact wraparound."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_multixact_freeze_max_age,
			.boot_val = 400000000,
			.min = 10000,
			.max = 2000000000,
		},
	},

	{
		.name = "autovacuum_multixact_freeze_score_weight",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_multixact_freeze_score_weight" short description */
		.short_desc = gettext_noop("Scaling factor of multixact freeze score for autovacuum prioritization."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_multixact_freeze_score_weight,
			.boot_val = 1.0,
			.min = 0.0,
			.max = 10.0,
		},
	},

	{
		.name = "autovacuum_naptime",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_naptime" short description */
		.short_desc = gettext_noop("Time to sleep between autovacuum runs."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_naptime,
			.boot_val = 60,
			.min = 1,
			.max = INT_MAX / 1000,
		},
	},

	{
		.name = "autovacuum_vacuum_cost_delay",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_cost_delay" short description */
		.short_desc = gettext_noop("Vacuum cost delay in milliseconds, for autovacuum."),
		/* translator: GUC parameter "autovacuum_vacuum_cost_delay" long description */
		.long_desc = gettext_noop("-1 means use \"vacuum_cost_delay\"."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_vac_cost_delay,
			.boot_val = 2,
			.min = -1,
			.max = 100,
		},
	},

	{
		.name = "autovacuum_vacuum_cost_limit",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_cost_limit" short description */
		.short_desc = gettext_noop("Vacuum cost amount available before napping, for autovacuum."),
		/* translator: GUC parameter "autovacuum_vacuum_cost_limit" long description */
		.long_desc = gettext_noop("-1 means use \"vacuum_cost_limit\"."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_vac_cost_limit,
			.boot_val = -1,
			.min = -1,
			.max = 10000,
		},
	},

	{
		.name = "autovacuum_vacuum_insert_scale_factor",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_insert_scale_factor" short description */
		.short_desc = gettext_noop("Number of tuple inserts prior to vacuum as a fraction of reltuples."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_vac_ins_scale,
			.boot_val = 0.2,
			.min = 0.0,
			.max = 100.0,
		},
	},

	{
		.name = "autovacuum_vacuum_insert_score_weight",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_insert_score_weight" short description */
		.short_desc = gettext_noop("Scaling factor of vacuum insert score for autovacuum prioritization."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_vacuum_insert_score_weight,
			.boot_val = 1.0,
			.min = 0.0,
			.max = 10.0,
		},
	},

	{
		.name = "autovacuum_vacuum_insert_threshold",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_insert_threshold" short description */
		.short_desc = gettext_noop("Minimum number of tuple inserts prior to vacuum."),
		/* translator: GUC parameter "autovacuum_vacuum_insert_threshold" long description */
		.long_desc = gettext_noop("-1 disables insert vacuums."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_vac_ins_thresh,
			.boot_val = 1000,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "autovacuum_vacuum_max_threshold",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_max_threshold" short description */
		.short_desc = gettext_noop("Maximum number of tuple updates or deletes prior to vacuum."),
		/* translator: GUC parameter "autovacuum_vacuum_max_threshold" long description */
		.long_desc = gettext_noop("-1 disables the maximum threshold."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_vac_max_thresh,
			.boot_val = 100000000,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "autovacuum_vacuum_scale_factor",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_scale_factor" short description */
		.short_desc = gettext_noop("Number of tuple updates or deletes prior to vacuum as a fraction of reltuples."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_vac_scale,
			.boot_val = 0.2,
			.min = 0.0,
			.max = 100.0,
		},
	},

	{
		.name = "autovacuum_vacuum_score_weight",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_score_weight" short description */
		.short_desc = gettext_noop("Scaling factor of vacuum score for autovacuum prioritization."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &autovacuum_vacuum_score_weight,
			.boot_val = 1.0,
			.min = 0.0,
			.max = 10.0,
		},
	},

	{
		.name = "autovacuum_vacuum_threshold",
		.context = PGC_SIGHUP,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_vacuum_threshold" short description */
		.short_desc = gettext_noop("Minimum number of tuple updates or deletes prior to vacuum."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_vac_thresh,
			.boot_val = 50,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "autovacuum_work_mem",
		.context = PGC_SIGHUP,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "autovacuum_work_mem" short description */
		.short_desc = gettext_noop("Sets the maximum memory to be used by each autovacuum worker process."),
		/* translator: GUC parameter "autovacuum_work_mem" long description */
		.long_desc = gettext_noop("-1 means use \"maintenance_work_mem\"."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_work_mem,
			.boot_val = -1,
			.min = -1,
			.max = MAX_KILOBYTES,
			.check_hook = check_autovacuum_work_mem,
		},
	},

	{
		.name = "autovacuum_worker_slots",
		.context = PGC_POSTMASTER,
		.group = VACUUM_AUTOVACUUM,
		/* translator: GUC parameter "autovacuum_worker_slots" short description */
		.short_desc = gettext_noop("Sets the number of backend slots to allocate for autovacuum workers."),
		.vartype = PGC_INT,
		._int = {
			.variable = &autovacuum_worker_slots,
			.boot_val = 16,
			.min = 1,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "backend_flush_after",
		.context = PGC_USERSET,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "backend_flush_after" short description */
		.short_desc = gettext_noop("Number of pages after which previously performed writes are flushed to disk."),
		/* translator: GUC parameter "backend_flush_after" long description */
		.long_desc = gettext_noop("0 disables forced writeback."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &backend_flush_after,
			.boot_val = DEFAULT_BACKEND_FLUSH_AFTER,
			.min = 0,
			.max = WRITEBACK_MAX_PENDING_FLUSHES,
		},
	},

	{
		.name = "backslash_quote",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "backslash_quote" short description */
		.short_desc = gettext_noop("Sets whether \"\\'\" is allowed in string literals."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &backslash_quote,
			.boot_val = BACKSLASH_QUOTE_SAFE_ENCODING,
			.options = backslash_quote_options,
		},
	},

	{
		.name = "backtrace_functions",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "backtrace_functions" short description */
		.short_desc = gettext_noop("Log backtrace for errors in these functions."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &backtrace_functions,
			.boot_val = "",
			.check_hook = check_backtrace_functions,
			.assign_hook = assign_backtrace_functions,
		},
	},

	{
		.name = "bgwriter_delay",
		.context = PGC_SIGHUP,
		.group = RESOURCES_BGWRITER,
		/* translator: GUC parameter "bgwriter_delay" short description */
		.short_desc = gettext_noop("Background writer sleep time between rounds."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &BgWriterDelay,
			.boot_val = 200,
			.min = 10,
			.max = 10000,
		},
	},

	{
		.name = "bgwriter_flush_after",
		.context = PGC_SIGHUP,
		.group = RESOURCES_BGWRITER,
		/* translator: GUC parameter "bgwriter_flush_after" short description */
		.short_desc = gettext_noop("Number of pages after which previously performed writes are flushed to disk."),
		/* translator: GUC parameter "bgwriter_flush_after" long description */
		.long_desc = gettext_noop("0 disables forced writeback."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &bgwriter_flush_after,
			.boot_val = DEFAULT_BGWRITER_FLUSH_AFTER,
			.min = 0,
			.max = WRITEBACK_MAX_PENDING_FLUSHES,
		},
	},

	{
		.name = "bgwriter_lru_maxpages",
		.context = PGC_SIGHUP,
		.group = RESOURCES_BGWRITER,
		/* translator: GUC parameter "bgwriter_lru_maxpages" short description */
		.short_desc = gettext_noop("Background writer maximum number of LRU pages to flush per round."),
		/* translator: GUC parameter "bgwriter_lru_maxpages" long description */
		.long_desc = gettext_noop("0 disables background writing."),
		.vartype = PGC_INT,
		._int = {
			.variable = &bgwriter_lru_maxpages,
			.boot_val = 100,
			.min = 0,
			.max = INT_MAX / 2,
		},
	},

	{
		.name = "bgwriter_lru_multiplier",
		.context = PGC_SIGHUP,
		.group = RESOURCES_BGWRITER,
		/* translator: GUC parameter "bgwriter_lru_multiplier" short description */
		.short_desc = gettext_noop("Multiple of the average buffer usage to free per round."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &bgwriter_lru_multiplier,
			.boot_val = 2.0,
			.min = 0.0,
			.max = 10.0,
		},
	},

	{
		.name = "block_size",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "block_size" short description */
		.short_desc = gettext_noop("Shows the size of a disk block."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &block_size,
			.boot_val = BLCKSZ,
			.min = BLCKSZ,
			.max = BLCKSZ,
		},
	},

	{
		.name = "bonjour",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "bonjour" short description */
		.short_desc = gettext_noop("Enables advertising the server via Bonjour."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_bonjour,
			.boot_val = false,
			.check_hook = check_bonjour,
		},
	},

	{
		.name = "bonjour_name",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "bonjour_name" short description */
		.short_desc = gettext_noop("Sets the Bonjour service name."),
		/* translator: GUC parameter "bonjour_name" long description */
		.long_desc = gettext_noop("An empty string means use the computer name."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &bonjour_name,
			.boot_val = "",
		},
	},

	{
		.name = "bytea_output",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "bytea_output" short description */
		.short_desc = gettext_noop("Sets the output format for bytea."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &bytea_output,
			.boot_val = BYTEA_OUTPUT_HEX,
			.options = bytea_output_options,
		},
	},

	{
		.name = "check_function_bodies",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "check_function_bodies" short description */
		.short_desc = gettext_noop("Check routine bodies during CREATE FUNCTION and CREATE PROCEDURE."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &check_function_bodies,
			.boot_val = true,
		},
	},

	{
		.name = "checkpoint_completion_target",
		.context = PGC_SIGHUP,
		.group = WAL_CHECKPOINTS,
		/* translator: GUC parameter "checkpoint_completion_target" short description */
		.short_desc = gettext_noop("Time spent flushing dirty buffers during checkpoint, as fraction of checkpoint interval."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &CheckPointCompletionTarget,
			.boot_val = 0.9,
			.min = 0.0,
			.max = 1.0,
			.assign_hook = assign_checkpoint_completion_target,
		},
	},

	{
		.name = "checkpoint_flush_after",
		.context = PGC_SIGHUP,
		.group = WAL_CHECKPOINTS,
		/* translator: GUC parameter "checkpoint_flush_after" short description */
		.short_desc = gettext_noop("Number of pages after which previously performed writes are flushed to disk."),
		/* translator: GUC parameter "checkpoint_flush_after" long description */
		.long_desc = gettext_noop("0 disables forced writeback."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &checkpoint_flush_after,
			.boot_val = DEFAULT_CHECKPOINT_FLUSH_AFTER,
			.min = 0,
			.max = WRITEBACK_MAX_PENDING_FLUSHES,
		},
	},

	{
		.name = "checkpoint_timeout",
		.context = PGC_SIGHUP,
		.group = WAL_CHECKPOINTS,
		/* translator: GUC parameter "checkpoint_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum time between automatic WAL checkpoints."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &CheckPointTimeout,
			.boot_val = 300,
			.min = 30,
			.max = 86400,
		},
	},

	{
		.name = "checkpoint_warning",
		.context = PGC_SIGHUP,
		.group = WAL_CHECKPOINTS,
		/* translator: GUC parameter "checkpoint_warning" short description */
		.short_desc = gettext_noop("Sets the maximum time before warning if checkpoints triggered by WAL volume happen too frequently."),
		/* translator: GUC parameter "checkpoint_warning" long description */
		.long_desc = gettext_noop("Write a message to the server log if checkpoints caused by the filling of WAL segment files happen more frequently than this amount of time. 0 disables the warning."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &CheckPointWarning,
			.boot_val = 30,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "client_connection_check_interval",
		.context = PGC_USERSET,
		.group = CONN_AUTH_TCP,
		/* translator: GUC parameter "client_connection_check_interval" short description */
		.short_desc = gettext_noop("Sets the time interval between checks for disconnection while running queries."),
		/* translator: GUC parameter "client_connection_check_interval" long description */
		.long_desc = gettext_noop("0 disables connection checks."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &client_connection_check_interval,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
			.check_hook = check_client_connection_check_interval,
		},
	},

	{
		.name = "client_encoding",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "client_encoding" short description */
		.short_desc = gettext_noop("Sets the client's character set encoding."),
		.flags = GUC_IS_NAME | GUC_REPORT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &client_encoding_string,
			.boot_val = "SQL_ASCII",
			.check_hook = check_client_encoding,
			.assign_hook = assign_client_encoding,
		},
	},

	{
		.name = "client_min_messages",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "client_min_messages" short description */
		.short_desc = gettext_noop("Sets the message levels that are sent to the client."),
		/* translator: GUC parameter "client_min_messages" long description */
		.long_desc = gettext_noop("Each level includes all the levels that follow it. The later the level, the fewer messages are sent."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &client_min_messages,
			.boot_val = NOTICE,
			.options = client_message_level_options,
		},
	},

	{
		.name = "cluster_name",
		.context = PGC_POSTMASTER,
		.group = PROCESS_TITLE,
		/* translator: GUC parameter "cluster_name" short description */
		.short_desc = gettext_noop("Sets the name of the cluster, which is included in the process title."),
		.flags = GUC_IS_NAME,
		.vartype = PGC_STRING,
		._string = {
			.variable = &cluster_name,
			.boot_val = "",
			.check_hook = check_cluster_name,
		},
	},

	{
		.name = "commit_delay",
		.context = PGC_SUSET,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "commit_delay" short description */
		.short_desc = gettext_noop("Sets the delay in microseconds between transaction commit and flushing WAL to disk."),
		.vartype = PGC_INT,
		._int = {
			.variable = &CommitDelay,
			.boot_val = 0,
			.min = 0,
			.max = 100000,
		},
	},

	{
		.name = "commit_siblings",
		.context = PGC_USERSET,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "commit_siblings" short description */
		.short_desc = gettext_noop("Sets the minimum number of concurrent open transactions required before performing \"commit_delay\"."),
		.vartype = PGC_INT,
		._int = {
			.variable = &CommitSiblings,
			.boot_val = 5,
			.min = 0,
			.max = 1000,
		},
	},

	{
		.name = "commit_timestamp_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "commit_timestamp_buffers" short description */
		.short_desc = gettext_noop("Sets the size of the dedicated buffer pool used for the commit timestamp cache."),
		/* translator: GUC parameter "commit_timestamp_buffers" long description */
		.long_desc = gettext_noop("0 means use a fraction of \"shared_buffers\"."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &commit_timestamp_buffers,
			.boot_val = 0,
			.min = 0,
			.max = SLRU_MAX_ALLOWED_BUFFERS,
			.check_hook = check_commit_ts_buffers,
		},
	},

	{
		.name = "compute_query_id",
		.context = PGC_SUSET,
		.group = STATS_MONITORING,
		/* translator: GUC parameter "compute_query_id" short description */
		.short_desc = gettext_noop("Enables in-core computation of query identifiers."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &compute_query_id,
			.boot_val = COMPUTE_QUERY_ID_AUTO,
			.options = compute_query_id_options,
		},
	},

	{
		.name = "config_file",
		.context = PGC_POSTMASTER,
		.group = FILE_LOCATIONS,
		/* translator: GUC parameter "config_file" short description */
		.short_desc = gettext_noop("Sets the server's main configuration file."),
		.flags = GUC_DISALLOW_IN_FILE | GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &ConfigFileName,
			.boot_val = NULL,
		},
	},

	{
		.name = "constraint_exclusion",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "constraint_exclusion" short description */
		.short_desc = gettext_noop("Enables the planner to use constraints to optimize queries."),
		/* translator: GUC parameter "constraint_exclusion" long description */
		.long_desc = gettext_noop("Table scans will be skipped if their constraints guarantee that no rows match the query."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &constraint_exclusion,
			.boot_val = CONSTRAINT_EXCLUSION_PARTITION,
			.options = constraint_exclusion_options,
		},
	},

	{
		.name = "cpu_index_tuple_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "cpu_index_tuple_cost" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the cost of processing each index entry during an index scan."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &cpu_index_tuple_cost,
			.boot_val = DEFAULT_CPU_INDEX_TUPLE_COST,
			.min = 0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "cpu_operator_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "cpu_operator_cost" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the cost of processing each operator or function call."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &cpu_operator_cost,
			.boot_val = DEFAULT_CPU_OPERATOR_COST,
			.min = 0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "cpu_tuple_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "cpu_tuple_cost" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the cost of processing each tuple (row)."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &cpu_tuple_cost,
			.boot_val = DEFAULT_CPU_TUPLE_COST,
			.min = 0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "createrole_self_grant",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "createrole_self_grant" short description */
		.short_desc = gettext_noop("Sets whether a CREATEROLE user automatically grants the role to themselves, and with which options."),
		/* translator: GUC parameter "createrole_self_grant" long description */
		.long_desc = gettext_noop("An empty string disables automatic self grants."),
		.flags = GUC_LIST_INPUT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &createrole_self_grant,
			.boot_val = "",
			.check_hook = check_createrole_self_grant,
			.assign_hook = assign_createrole_self_grant,
		},
	},

	{
		.name = "cursor_tuple_fraction",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "cursor_tuple_fraction" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the fraction of a cursor's rows that will be retrieved."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &cursor_tuple_fraction,
			.boot_val = DEFAULT_CURSOR_TUPLE_FRACTION,
			.min = 0.0,
			.max = 1.0,
		},
	},

	{
		.name = "data_checksums",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "data_checksums" short description */
		.short_desc = gettext_noop("Shows whether data checksums are turned on for this cluster."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_RUNTIME_COMPUTED,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &data_checksums,
			.boot_val = PG_DATA_CHECKSUM_OFF,
			.options = data_checksums_options,
			.show_hook = show_data_checksums,
		},
	},

	{
		.name = "data_directory",
		.context = PGC_POSTMASTER,
		.group = FILE_LOCATIONS,
		/* translator: GUC parameter "data_directory" short description */
		.short_desc = gettext_noop("Sets the server's data directory."),
		.flags = GUC_SUPERUSER_ONLY | GUC_DISALLOW_IN_AUTO_FILE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &data_directory,
			.boot_val = NULL,
		},
	},

	{
		.name = "data_directory_mode",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "data_directory_mode" short description */
		.short_desc = gettext_noop("Shows the mode of the data directory."),
		/* translator: GUC parameter "data_directory_mode" long description */
		.long_desc = gettext_noop("The parameter value is a numeric mode specification in the form accepted by the chmod and umask system calls. (To use the customary octal format the number must start with a 0 (zero).)"),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_RUNTIME_COMPUTED | GUC_SHOW_IN_OCTAL,
		.vartype = PGC_INT,
		._int = {
			.variable = &data_directory_mode,
			.boot_val = 0700,
			.min = 0000,
			.max = 0777,
		},
	},

	{
		.name = "data_sync_retry",
		.context = PGC_POSTMASTER,
		.group = ERROR_HANDLING_OPTIONS,
		/* translator: GUC parameter "data_sync_retry" short description */
		.short_desc = gettext_noop("Whether to continue running after a failure to sync data files."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &data_sync_retry,
			.boot_val = false,
		},
	},

	{
		.name = "DateStyle",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "DateStyle" short description */
		.short_desc = gettext_noop("Sets the display format for date and time values."),
		/* translator: GUC parameter "DateStyle" long description */
		.long_desc = gettext_noop("Also controls interpretation of ambiguous date inputs."),
		.flags = GUC_LIST_INPUT | GUC_REPORT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &datestyle_string,
			.boot_val = "ISO, MDY",
			.check_hook = check_datestyle,
			.assign_hook = assign_datestyle,
		},
	},

	{
		.name = "deadlock_timeout",
		.context = PGC_SUSET,
		.group = LOCK_MANAGEMENT,
		/* translator: GUC parameter "deadlock_timeout" short description */
		.short_desc = gettext_noop("Sets the time to wait on a lock before checking for deadlock."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &DeadlockTimeout,
			.boot_val = 1000,
			.min = 1,
			.max = INT_MAX,
		},
	},

	{
		.name = "debug_assertions",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "debug_assertions" short description */
		.short_desc = gettext_noop("Shows whether the running server has assertion checks enabled."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &assert_enabled,
			.boot_val = DEFAULT_ASSERT_ENABLED,
		},
	},

#ifdef DEBUG_NODE_TESTS_ENABLED
	{
		.name = "debug_copy_parse_plan_trees",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_copy_parse_plan_trees" short description */
		.short_desc = gettext_noop("Set this to force all parse and plan trees to be passed through copyObject(), to facilitate catching errors and omissions in copyObject()."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_copy_parse_plan_trees,
			.boot_val = DEFAULT_DEBUG_COPY_PARSE_PLAN_TREES,
		},
	},
#endif

#ifdef LOCK_DEBUG
	{
		.name = "debug_deadlocks",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_deadlocks" short description */
		.short_desc = gettext_noop("Dumps information about all current locks when a deadlock timeout occurs."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_deadlocks,
			.boot_val = false,
		},
	},
#endif

	{
		.name = "debug_discard_caches",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_discard_caches" short description */
		.short_desc = gettext_noop("Aggressively flush system caches for debugging purposes."),
		/* translator: GUC parameter "debug_discard_caches" long description */
		.long_desc = gettext_noop("0 means use normal caching behavior."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_INT,
		._int = {
			.variable = &debug_discard_caches,
			.boot_val = DEFAULT_DEBUG_DISCARD_CACHES,
			.min = MIN_DEBUG_DISCARD_CACHES,
			.max = MAX_DEBUG_DISCARD_CACHES,
		},
	},

	{
		.name = "debug_exec_backend",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "debug_exec_backend" short description */
		.short_desc = gettext_noop("Shows whether the running server is built with EXEC_BACKEND enabled."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &exec_backend_enabled,
			.boot_val = EXEC_BACKEND_ENABLED,
		},
	},

	{
		.name = "debug_io_direct",
		.context = PGC_POSTMASTER,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_io_direct" short description */
		.short_desc = gettext_noop("Use direct I/O for file access."),
		/* translator: GUC parameter "debug_io_direct" long description */
		.long_desc = gettext_noop("An empty string disables direct I/O."),
		.flags = GUC_LIST_INPUT | GUC_NOT_IN_SAMPLE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &debug_io_direct_string,
			.boot_val = "",
			.check_hook = check_debug_io_direct,
			.assign_hook = assign_debug_io_direct,
		},
	},

	{
		.name = "debug_logical_replication_streaming",
		.context = PGC_USERSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_logical_replication_streaming" short description */
		.short_desc = gettext_noop("Forces immediate streaming or serialization of changes in large transactions."),
		/* translator: GUC parameter "debug_logical_replication_streaming" long description */
		.long_desc = gettext_noop("On the publisher, it allows streaming or serializing each change in logical decoding. On the subscriber, it allows serialization of all changes to files and notifies the parallel apply workers to read and apply them at the end of the transaction."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &debug_logical_replication_streaming,
			.boot_val = DEBUG_LOGICAL_REP_STREAMING_BUFFERED,
			.options = debug_logical_replication_streaming_options,
		},
	},

	{
		.name = "debug_parallel_query",
		.context = PGC_USERSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_parallel_query" short description */
		.short_desc = gettext_noop("Forces the planner's use parallel query nodes."),
		/* translator: GUC parameter "debug_parallel_query" long description */
		.long_desc = gettext_noop("This can be useful for testing the parallel query infrastructure by forcing the planner to generate plans that contain nodes that perform tuple communication between workers and the main process."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_EXPLAIN,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &debug_parallel_query,
			.boot_val = DEBUG_PARALLEL_OFF,
			.options = debug_parallel_query_options,
		},
	},

	{
		.name = "debug_pretty_print",
		.context = PGC_USERSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "debug_pretty_print" short description */
		.short_desc = gettext_noop("Indents parse and plan tree displays."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_pretty_print,
			.boot_val = true,
		},
	},

	{
		.name = "debug_print_parse",
		.context = PGC_USERSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "debug_print_parse" short description */
		.short_desc = gettext_noop("Logs each query's parse tree."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_print_parse,
			.boot_val = false,
		},
	},

	{
		.name = "debug_print_plan",
		.context = PGC_USERSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "debug_print_plan" short description */
		.short_desc = gettext_noop("Logs each query's execution plan."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_print_plan,
			.boot_val = false,
		},
	},

	{
		.name = "debug_print_raw_parse",
		.context = PGC_USERSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "debug_print_raw_parse" short description */
		.short_desc = gettext_noop("Logs each query's raw parse tree."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_print_raw_parse,
			.boot_val = false,
		},
	},

	{
		.name = "debug_print_rewritten",
		.context = PGC_USERSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "debug_print_rewritten" short description */
		.short_desc = gettext_noop("Logs each query's rewritten parse tree."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_print_rewritten,
			.boot_val = false,
		},
	},

#ifdef DEBUG_NODE_TESTS_ENABLED
	{
		.name = "debug_raw_expression_coverage_test",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_raw_expression_coverage_test" short description */
		.short_desc = gettext_noop("Set this to force all raw parse trees for DML statements to be scanned by raw_expression_tree_walker(), to facilitate catching errors and omissions in that function."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_raw_expression_coverage_test,
			.boot_val = DEFAULT_DEBUG_RAW_EXPRESSION_COVERAGE_TEST,
		},
	},
#endif

#ifdef DEBUG_NODE_TESTS_ENABLED
	{
		.name = "debug_write_read_parse_plan_trees",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "debug_write_read_parse_plan_trees" short description */
		.short_desc = gettext_noop("Set this to force all parse and plan trees to be passed through outfuncs.c/readfuncs.c, to facilitate catching errors and omissions in those modules."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Debug_write_read_parse_plan_trees,
			.boot_val = DEFAULT_DEBUG_WRITE_READ_PARSE_PLAN_TREES,
		},
	},
#endif

	{
		.name = "default_statistics_target",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "default_statistics_target" short description */
		.short_desc = gettext_noop("Sets the default statistics target."),
		/* translator: GUC parameter "default_statistics_target" long description */
		.long_desc = gettext_noop("This applies to table columns that have not had a column-specific target set via ALTER TABLE SET STATISTICS."),
		.vartype = PGC_INT,
		._int = {
			.variable = &default_statistics_target,
			.boot_val = 100,
			.min = 1,
			.max = MAX_STATISTICS_TARGET,
		},
	},

	{
		.name = "default_table_access_method",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "default_table_access_method" short description */
		.short_desc = gettext_noop("Sets the default table access method for new tables."),
		.flags = GUC_IS_NAME,
		.vartype = PGC_STRING,
		._string = {
			.variable = &default_table_access_method,
			.boot_val = DEFAULT_TABLE_ACCESS_METHOD,
			.check_hook = check_default_table_access_method,
		},
	},

	{
		.name = "default_tablespace",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "default_tablespace" short description */
		.short_desc = gettext_noop("Sets the default tablespace to create tables and indexes in."),
		/* translator: GUC parameter "default_tablespace" long description */
		.long_desc = gettext_noop("An empty string means use the database's default tablespace."),
		.flags = GUC_IS_NAME,
		.vartype = PGC_STRING,
		._string = {
			.variable = &default_tablespace,
			.boot_val = "",
			.check_hook = check_default_tablespace,
		},
	},

	{
		.name = "default_text_search_config",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "default_text_search_config" short description */
		.short_desc = gettext_noop("Sets default text search configuration."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &TSCurrentConfig,
			.boot_val = "pg_catalog.simple",
			.check_hook = check_default_text_search_config,
			.assign_hook = assign_default_text_search_config,
		},
	},

	{
		.name = "default_toast_compression",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "default_toast_compression" short description */
		.short_desc = gettext_noop("Sets the default compression method for compressible values."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &default_toast_compression,
			.boot_val = DEFAULT_TOAST_COMPRESSION,
			.options = default_toast_compression_options,
		},
	},

	{
		.name = "default_transaction_deferrable",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "default_transaction_deferrable" short description */
		.short_desc = gettext_noop("Sets the default deferrable status of new transactions."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &DefaultXactDeferrable,
			.boot_val = false,
		},
	},

	{
		.name = "default_transaction_isolation",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "default_transaction_isolation" short description */
		.short_desc = gettext_noop("Sets the transaction isolation level of each new transaction."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &DefaultXactIsoLevel,
			.boot_val = XACT_READ_COMMITTED,
			.options = isolation_level_options,
		},
	},

	{
		.name = "default_transaction_read_only",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "default_transaction_read_only" short description */
		.short_desc = gettext_noop("Sets the default read-only status of new transactions."),
		.flags = GUC_REPORT,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &DefaultXactReadOnly,
			.boot_val = false,
		},
	},

	{
		.name = "default_with_oids",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "default_with_oids" short description */
		.short_desc = gettext_noop("WITH OIDS is no longer supported; this can only be false."),
		.flags = GUC_NO_SHOW_ALL | GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &default_with_oids,
			.boot_val = false,
			.check_hook = check_default_with_oids,
		},
	},

	{
		.name = "dynamic_library_path",
		.context = PGC_SUSET,
		.group = CLIENT_CONN_OTHER,
		/* translator: GUC parameter "dynamic_library_path" short description */
		.short_desc = gettext_noop("Sets the path for dynamically loadable modules."),
		/* translator: GUC parameter "dynamic_library_path" long description */
		.long_desc = gettext_noop("If a dynamically loadable module needs to be opened and the specified name does not have a directory component (i.e., the name does not contain a slash), the system will search this path for the specified file."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &Dynamic_library_path,
			.boot_val = "$libdir",
		},
	},

	{
		.name = "dynamic_shared_memory_type",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "dynamic_shared_memory_type" short description */
		.short_desc = gettext_noop("Selects the dynamic shared memory implementation used."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &dynamic_shared_memory_type,
			.boot_val = DEFAULT_DYNAMIC_SHARED_MEMORY_TYPE,
			.options = dynamic_shared_memory_options,
		},
	},

	{
		.name = "effective_cache_size",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "effective_cache_size" short description */
		.short_desc = gettext_noop("Sets the planner's assumption about the total size of the data caches."),
		/* translator: GUC parameter "effective_cache_size" long description */
		.long_desc = gettext_noop("That is, the total size of the caches (kernel cache and shared buffers) used for PostgreSQL data files. This is measured in disk pages, which are normally 8 kB each."),
		.flags = GUC_UNIT_BLOCKS | GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &effective_cache_size,
			.boot_val = DEFAULT_EFFECTIVE_CACHE_SIZE,
			.min = 1,
			.max = INT_MAX,
		},
	},

	{
		.name = "effective_io_concurrency",
		.context = PGC_USERSET,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "effective_io_concurrency" short description */
		.short_desc = gettext_noop("Number of simultaneous requests that can be handled efficiently by the disk subsystem."),
		/* translator: GUC parameter "effective_io_concurrency" long description */
		.long_desc = gettext_noop("0 disables simultaneous requests."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &effective_io_concurrency,
			.boot_val = DEFAULT_EFFECTIVE_IO_CONCURRENCY,
			.min = 0,
			.max = MAX_IO_CONCURRENCY,
		},
	},

	{
		.name = "effective_wal_level",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "effective_wal_level" short description */
		.short_desc = gettext_noop("Shows effective WAL level."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &effective_wal_level,
			.boot_val = WAL_LEVEL_REPLICA,
			.options = wal_level_options,
			.show_hook = show_effective_wal_level,
		},
	},

	{
		.name = "enable_async_append",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_async_append" short description */
		.short_desc = gettext_noop("Enables the planner's use of async append plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_async_append,
			.boot_val = true,
		},
	},

	{
		.name = "enable_bitmapscan",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_bitmapscan" short description */
		.short_desc = gettext_noop("Enables the planner's use of bitmap-scan plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_bitmapscan,
			.boot_val = true,
		},
	},

	{
		.name = "enable_distinct_reordering",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_distinct_reordering" short description */
		.short_desc = gettext_noop("Enables reordering of DISTINCT keys."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_distinct_reordering,
			.boot_val = true,
		},
	},

	{
		.name = "enable_eager_aggregate",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_eager_aggregate" short description */
		.short_desc = gettext_noop("Enables eager aggregation."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_eager_aggregate,
			.boot_val = true,
		},
	},

	{
		.name = "enable_gathermerge",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_gathermerge" short description */
		.short_desc = gettext_noop("Enables the planner's use of gather merge plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_gathermerge,
			.boot_val = true,
		},
	},

	{
		.name = "enable_group_by_reordering",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_group_by_reordering" short description */
		.short_desc = gettext_noop("Enables reordering of GROUP BY keys."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_group_by_reordering,
			.boot_val = true,
		},
	},

	{
		.name = "enable_groupagg",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_groupagg" short description */
		.short_desc = gettext_noop("Enables the planner's use of sort-based grouping and aggregation plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_groupagg,
			.boot_val = true,
		},
	},

	{
		.name = "enable_hashagg",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_hashagg" short description */
		.short_desc = gettext_noop("Enables the planner's use of hashed aggregation plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_hashagg,
			.boot_val = true,
		},
	},

	{
		.name = "enable_hashjoin",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_hashjoin" short description */
		.short_desc = gettext_noop("Enables the planner's use of hash join plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_hashjoin,
			.boot_val = true,
		},
	},

	{
		.name = "enable_incremental_sort",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_incremental_sort" short description */
		.short_desc = gettext_noop("Enables the planner's use of incremental sort steps."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_incremental_sort,
			.boot_val = true,
		},
	},

	{
		.name = "enable_indexonlyscan",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_indexonlyscan" short description */
		.short_desc = gettext_noop("Enables the planner's use of index-only-scan plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_indexonlyscan,
			.boot_val = true,
		},
	},

	{
		.name = "enable_indexscan",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_indexscan" short description */
		.short_desc = gettext_noop("Enables the planner's use of index-scan plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_indexscan,
			.boot_val = true,
		},
	},

	{
		.name = "enable_material",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_material" short description */
		.short_desc = gettext_noop("Enables the planner's use of materialization."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_material,
			.boot_val = true,
		},
	},

	{
		.name = "enable_memoize",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_memoize" short description */
		.short_desc = gettext_noop("Enables the planner's use of memoization."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_memoize,
			.boot_val = true,
		},
	},

	{
		.name = "enable_mergejoin",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_mergejoin" short description */
		.short_desc = gettext_noop("Enables the planner's use of merge join plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_mergejoin,
			.boot_val = true,
		},
	},

	{
		.name = "enable_nestloop",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_nestloop" short description */
		.short_desc = gettext_noop("Enables the planner's use of nested-loop join plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_nestloop,
			.boot_val = true,
		},
	},

	{
		.name = "enable_parallel_append",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_parallel_append" short description */
		.short_desc = gettext_noop("Enables the planner's use of parallel append plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_parallel_append,
			.boot_val = true,
		},
	},

	{
		.name = "enable_parallel_hash",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_parallel_hash" short description */
		.short_desc = gettext_noop("Enables the planner's use of parallel hash plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_parallel_hash,
			.boot_val = true,
		},
	},

	{
		.name = "enable_partition_pruning",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_partition_pruning" short description */
		.short_desc = gettext_noop("Enables plan-time and execution-time partition pruning."),
		/* translator: GUC parameter "enable_partition_pruning" long description */
		.long_desc = gettext_noop("Allows the query planner and executor to compare partition bounds to conditions in the query to determine which partitions must be scanned."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_partition_pruning,
			.boot_val = true,
		},
	},

	{
		.name = "enable_partitionwise_aggregate",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_partitionwise_aggregate" short description */
		.short_desc = gettext_noop("Enables partitionwise aggregation and grouping."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_partitionwise_aggregate,
			.boot_val = false,
		},
	},

	{
		.name = "enable_partitionwise_join",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_partitionwise_join" short description */
		.short_desc = gettext_noop("Enables partitionwise join."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_partitionwise_join,
			.boot_val = false,
		},
	},

	{
		.name = "enable_presorted_aggregate",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_presorted_aggregate" short description */
		.short_desc = gettext_noop("Enables the planner's ability to produce plans that provide presorted input for ORDER BY / DISTINCT aggregate functions."),
		/* translator: GUC parameter "enable_presorted_aggregate" long description */
		.long_desc = gettext_noop("Allows the query planner to build plans that provide presorted input for aggregate functions with an ORDER BY / DISTINCT clause.  When disabled, implicit sorts are always performed during execution."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_presorted_aggregate,
			.boot_val = true,
		},
	},

	{
		.name = "enable_self_join_elimination",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_self_join_elimination" short description */
		.short_desc = gettext_noop("Enables removal of unique self-joins."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_self_join_elimination,
			.boot_val = true,
		},
	},

	{
		.name = "enable_seqscan",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_seqscan" short description */
		.short_desc = gettext_noop("Enables the planner's use of sequential-scan plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_seqscan,
			.boot_val = true,
		},
	},

	{
		.name = "enable_sort",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_sort" short description */
		.short_desc = gettext_noop("Enables the planner's use of explicit sort steps."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_sort,
			.boot_val = true,
		},
	},

	{
		.name = "enable_tidscan",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "enable_tidscan" short description */
		.short_desc = gettext_noop("Enables the planner's use of TID scan plans."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_tidscan,
			.boot_val = true,
		},
	},

	{
		.name = "event_source",
		.context = PGC_POSTMASTER,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "event_source" short description */
		.short_desc = gettext_noop("Sets the application name used to identify PostgreSQL messages in the event log."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &event_source,
			.boot_val = DEFAULT_EVENT_SOURCE,
		},
	},

	{
		.name = "event_triggers",
		.context = PGC_SUSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "event_triggers" short description */
		.short_desc = gettext_noop("Enables event triggers."),
		/* translator: GUC parameter "event_triggers" long description */
		.long_desc = gettext_noop("When enabled, event triggers will fire for all applicable statements."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &event_triggers,
			.boot_val = true,
		},
	},

	{
		.name = "exit_on_error",
		.context = PGC_USERSET,
		.group = ERROR_HANDLING_OPTIONS,
		/* translator: GUC parameter "exit_on_error" short description */
		.short_desc = gettext_noop("Terminate session on any error."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &ExitOnAnyError,
			.boot_val = false,
		},
	},

	{
		.name = "extension_control_path",
		.context = PGC_SUSET,
		.group = CLIENT_CONN_OTHER,
		/* translator: GUC parameter "extension_control_path" short description */
		.short_desc = gettext_noop("Sets the path for extension control files."),
		/* translator: GUC parameter "extension_control_path" long description */
		.long_desc = gettext_noop("The remaining extension script and secondary control files are then loaded from the same directory where the primary control file was found."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &Extension_control_path,
			.boot_val = "$system",
		},
	},

	{
		.name = "external_pid_file",
		.context = PGC_POSTMASTER,
		.group = FILE_LOCATIONS,
		/* translator: GUC parameter "external_pid_file" short description */
		.short_desc = gettext_noop("Writes the postmaster PID to the specified file."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &external_pid_file,
			.boot_val = NULL,
			.check_hook = check_canonical_path,
		},
	},

	{
		.name = "extra_float_digits",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "extra_float_digits" short description */
		.short_desc = gettext_noop("Sets the number of digits displayed for floating-point values."),
		/* translator: GUC parameter "extra_float_digits" long description */
		.long_desc = gettext_noop("This affects real, double precision, and geometric data types. A zero or negative parameter value is added to the standard number of digits (FLT_DIG or DBL_DIG as appropriate). Any value greater than zero selects precise output mode."),
		.vartype = PGC_INT,
		._int = {
			.variable = &extra_float_digits,
			.boot_val = 1,
			.min = -15,
			.max = 3,
		},
	},

	{
		.name = "file_copy_method",
		.context = PGC_USERSET,
		.group = RESOURCES_DISK,
		/* translator: GUC parameter "file_copy_method" short description */
		.short_desc = gettext_noop("Selects the file copy method."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &file_copy_method,
			.boot_val = FILE_COPY_METHOD_COPY,
			.options = file_copy_method_options,
		},
	},

	{
		.name = "file_extend_method",
		.context = PGC_SIGHUP,
		.group = RESOURCES_DISK,
		/* translator: GUC parameter "file_extend_method" short description */
		.short_desc = gettext_noop("Selects the method used for extending data files."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &file_extend_method,
			.boot_val = DEFAULT_FILE_EXTEND_METHOD,
			.options = file_extend_method_options,
		},
	},

	{
		.name = "from_collapse_limit",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "from_collapse_limit" short description */
		.short_desc = gettext_noop("Sets the FROM-list size beyond which subqueries are not collapsed."),
		/* translator: GUC parameter "from_collapse_limit" long description */
		.long_desc = gettext_noop("The planner will merge subqueries into upper queries if the resulting FROM list would have no more than this many items."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &from_collapse_limit,
			.boot_val = 8,
			.min = 1,
			.max = INT_MAX,
		},
	},

	{
		.name = "fsync",
		.context = PGC_SIGHUP,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "fsync" short description */
		.short_desc = gettext_noop("Forces synchronization of updates to disk."),
		/* translator: GUC parameter "fsync" long description */
		.long_desc = gettext_noop("The server will use the fsync() system call in several places to make sure that updates are physically written to disk. This ensures that a database cluster will recover to a consistent state after an operating system or hardware crash."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enableFsync,
			.boot_val = true,
		},
	},

	{
		.name = "full_page_writes",
		.context = PGC_SIGHUP,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "full_page_writes" short description */
		.short_desc = gettext_noop("Writes full pages to WAL when first modified after a checkpoint."),
		/* translator: GUC parameter "full_page_writes" long description */
		.long_desc = gettext_noop("A page write in process during an operating system crash might be only partially written to disk.  During recovery, the row changes stored in WAL are not enough to recover.  This option writes pages when first modified after a checkpoint to WAL so full recovery is possible."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &fullPageWrites,
			.boot_val = true,
		},
	},

	{
		.name = "geqo",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_GEQO,
		/* translator: GUC parameter "geqo" short description */
		.short_desc = gettext_noop("Enables genetic query optimization."),
		/* translator: GUC parameter "geqo" long description */
		.long_desc = gettext_noop("This algorithm attempts to do planning without exhaustive searching."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &enable_geqo,
			.boot_val = true,
		},
	},

	{
		.name = "geqo_effort",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_GEQO,
		/* translator: GUC parameter "geqo_effort" short description */
		.short_desc = gettext_noop("GEQO: effort is used to set the default for other GEQO parameters."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &Geqo_effort,
			.boot_val = DEFAULT_GEQO_EFFORT,
			.min = MIN_GEQO_EFFORT,
			.max = MAX_GEQO_EFFORT,
		},
	},

	{
		.name = "geqo_generations",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_GEQO,
		/* translator: GUC parameter "geqo_generations" short description */
		.short_desc = gettext_noop("GEQO: number of iterations of the algorithm."),
		/* translator: GUC parameter "geqo_generations" long description */
		.long_desc = gettext_noop("0 means use a suitable default value."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &Geqo_generations,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "geqo_pool_size",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_GEQO,
		/* translator: GUC parameter "geqo_pool_size" short description */
		.short_desc = gettext_noop("GEQO: number of individuals in the population."),
		/* translator: GUC parameter "geqo_pool_size" long description */
		.long_desc = gettext_noop("0 means use a suitable default value."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &Geqo_pool_size,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "geqo_seed",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_GEQO,
		/* translator: GUC parameter "geqo_seed" short description */
		.short_desc = gettext_noop("GEQO: seed for random path selection."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &Geqo_seed,
			.boot_val = 0.0,
			.min = 0.0,
			.max = 1.0,
		},
	},

	{
		.name = "geqo_selection_bias",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_GEQO,
		/* translator: GUC parameter "geqo_selection_bias" short description */
		.short_desc = gettext_noop("GEQO: selective pressure within the population."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &Geqo_selection_bias,
			.boot_val = DEFAULT_GEQO_SELECTION_BIAS,
			.min = MIN_GEQO_SELECTION_BIAS,
			.max = MAX_GEQO_SELECTION_BIAS,
		},
	},

	{
		.name = "geqo_threshold",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_GEQO,
		/* translator: GUC parameter "geqo_threshold" short description */
		.short_desc = gettext_noop("Sets the threshold of FROM items beyond which GEQO is used."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &geqo_threshold,
			.boot_val = 12,
			.min = 2,
			.max = INT_MAX,
		},
	},

	{
		.name = "gin_fuzzy_search_limit",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_OTHER,
		/* translator: GUC parameter "gin_fuzzy_search_limit" short description */
		.short_desc = gettext_noop("Sets the maximum allowed result for exact search by GIN."),
		/* translator: GUC parameter "gin_fuzzy_search_limit" long description */
		.long_desc = gettext_noop("0 means no limit."),
		.vartype = PGC_INT,
		._int = {
			.variable = &GinFuzzySearchLimit,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "gin_pending_list_limit",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "gin_pending_list_limit" short description */
		.short_desc = gettext_noop("Sets the maximum size of the pending list for GIN index."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &gin_pending_list_limit,
			.boot_val = 4096,
			.min = 64,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "gss_accept_delegation",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "gss_accept_delegation" short description */
		.short_desc = gettext_noop("Sets whether GSSAPI delegation should be accepted from the client."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &pg_gss_accept_delegation,
			.boot_val = false,
		},
	},

	{
		.name = "hash_mem_multiplier",
		.context = PGC_USERSET,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "hash_mem_multiplier" short description */
		.short_desc = gettext_noop("Multiple of \"work_mem\" to use for hash tables."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &hash_mem_multiplier,
			.boot_val = 2.0,
			.min = 1.0,
			.max = 1000.0,
		},
	},

	{
		.name = "hba_file",
		.context = PGC_POSTMASTER,
		.group = FILE_LOCATIONS,
		/* translator: GUC parameter "hba_file" short description */
		.short_desc = gettext_noop("Sets the server's \"hba\" configuration file."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &HbaFileName,
			.boot_val = NULL,
		},
	},

	{
		.name = "hosts_file",
		.context = PGC_POSTMASTER,
		.group = FILE_LOCATIONS,
		/* translator: GUC parameter "hosts_file" short description */
		.short_desc = gettext_noop("Sets the server's \"hosts\" configuration file."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &HostsFileName,
			.boot_val = NULL,
		},
	},

	{
		.name = "hot_standby",
		.context = PGC_POSTMASTER,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "hot_standby" short description */
		.short_desc = gettext_noop("Allows connections and queries during recovery."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &EnableHotStandby,
			.boot_val = true,
		},
	},

	{
		.name = "hot_standby_feedback",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "hot_standby_feedback" short description */
		.short_desc = gettext_noop("Allows feedback from a hot standby to the primary that will avoid query conflicts."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &hot_standby_feedback,
			.boot_val = false,
		},
	},

	{
		.name = "huge_page_size",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "huge_page_size" short description */
		.short_desc = gettext_noop("The size of huge page that should be requested."),
		/* translator: GUC parameter "huge_page_size" long description */
		.long_desc = gettext_noop("0 means use the system default."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &huge_page_size,
			.boot_val = 0,
			.min = 0,
			.max = MAX_KILOBYTES,
			.check_hook = check_huge_page_size,
		},
	},

	{
		.name = "huge_pages",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "huge_pages" short description */
		.short_desc = gettext_noop("Use of huge pages on Linux or Windows."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &huge_pages,
			.boot_val = HUGE_PAGES_TRY,
			.options = huge_pages_options,
		},
	},

	{
		.name = "huge_pages_status",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "huge_pages_status" short description */
		.short_desc = gettext_noop("Indicates the status of huge pages."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &huge_pages_status,
			.boot_val = HUGE_PAGES_UNKNOWN,
			.options = huge_pages_status_options,
		},
	},

	{
		.name = "icu_validation_level",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "icu_validation_level" short description */
		.short_desc = gettext_noop("Log level for reporting invalid ICU locale strings."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &icu_validation_level,
			.boot_val = WARNING,
			.options = icu_validation_level_options,
		},
	},

	{
		.name = "ident_file",
		.context = PGC_POSTMASTER,
		.group = FILE_LOCATIONS,
		/* translator: GUC parameter "ident_file" short description */
		.short_desc = gettext_noop("Sets the server's \"ident\" configuration file."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &IdentFileName,
			.boot_val = NULL,
		},
	},

	{
		.name = "idle_in_transaction_session_timeout",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "idle_in_transaction_session_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum allowed idle time between queries, when in a transaction."),
		/* translator: GUC parameter "idle_in_transaction_session_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &IdleInTransactionSessionTimeout,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "idle_replication_slot_timeout",
		.context = PGC_SIGHUP,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "idle_replication_slot_timeout" short description */
		.short_desc = gettext_noop("Sets the duration a replication slot can remain idle before it is invalidated."),
		/* translator: GUC parameter "idle_replication_slot_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &idle_replication_slot_timeout_secs,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "idle_session_timeout",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "idle_session_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum allowed idle time between queries, when not in a transaction."),
		/* translator: GUC parameter "idle_session_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &IdleSessionTimeout,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "ignore_checksum_failure",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "ignore_checksum_failure" short description */
		.short_desc = gettext_noop("Continues processing after a checksum failure."),
		/* translator: GUC parameter "ignore_checksum_failure" long description */
		.long_desc = gettext_noop("Detection of a checksum failure normally causes PostgreSQL to report an error, aborting the current transaction. Setting ignore_checksum_failure to true causes the system to ignore the failure (but still report a warning), and continue processing. This behavior could cause crashes or other serious problems. Only has an effect if checksums are enabled."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &ignore_checksum_failure,
			.boot_val = false,
		},
	},

	{
		.name = "ignore_invalid_pages",
		.context = PGC_POSTMASTER,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "ignore_invalid_pages" short description */
		.short_desc = gettext_noop("Continues recovery after an invalid pages failure."),
		/* translator: GUC parameter "ignore_invalid_pages" long description */
		.long_desc = gettext_noop("Detection of WAL records having references to invalid pages during recovery causes PostgreSQL to raise a PANIC-level error, aborting the recovery. Setting \"ignore_invalid_pages\" to true causes the system to ignore invalid page references in WAL records (but still report a warning), and continue recovery. This behavior may cause crashes, data loss, propagate or hide corruption, or other serious problems. Only has an effect during recovery or in standby mode."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &ignore_invalid_pages,
			.boot_val = false,
		},
	},

	{
		.name = "ignore_system_indexes",
		.context = PGC_BACKEND,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "ignore_system_indexes" short description */
		.short_desc = gettext_noop("Disables reading from system indexes."),
		/* translator: GUC parameter "ignore_system_indexes" long description */
		.long_desc = gettext_noop("It does not prevent updating the indexes, so it is safe to use.  The worst consequence is slowness."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &IgnoreSystemIndexes,
			.boot_val = false,
		},
	},

	{
		.name = "in_hot_standby",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "in_hot_standby" short description */
		.short_desc = gettext_noop("Shows whether hot standby is currently active."),
		.flags = GUC_REPORT | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &in_hot_standby_guc,
			.boot_val = false,
			.show_hook = show_in_hot_standby,
		},
	},

	{
		.name = "integer_datetimes",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "integer_datetimes" short description */
		.short_desc = gettext_noop("Shows whether datetimes are integer based."),
		.flags = GUC_REPORT | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &integer_datetimes,
			.boot_val = true,
		},
	},

	{
		.name = "IntervalStyle",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "IntervalStyle" short description */
		.short_desc = gettext_noop("Sets the display format for interval values."),
		.flags = GUC_REPORT,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &IntervalStyle,
			.boot_val = INTSTYLE_POSTGRES,
			.options = intervalstyle_options,
		},
	},

	{
		.name = "io_combine_limit",
		.context = PGC_USERSET,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_combine_limit" short description */
		.short_desc = gettext_noop("Limit on the size of data reads and writes."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &io_combine_limit_guc,
			.boot_val = DEFAULT_IO_COMBINE_LIMIT,
			.min = 1,
			.max = MAX_IO_COMBINE_LIMIT,
			.assign_hook = assign_io_combine_limit,
		},
	},

	{
		.name = "io_max_combine_limit",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_max_combine_limit" short description */
		.short_desc = gettext_noop("Server-wide limit that clamps io_combine_limit."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &io_max_combine_limit,
			.boot_val = DEFAULT_IO_COMBINE_LIMIT,
			.min = 1,
			.max = MAX_IO_COMBINE_LIMIT,
			.assign_hook = assign_io_max_combine_limit,
		},
	},

	{
		.name = "io_max_concurrency",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_max_concurrency" short description */
		.short_desc = gettext_noop("Max number of IOs that one process can execute simultaneously."),
		/* translator: GUC parameter "io_max_concurrency" long description */
		.long_desc = gettext_noop("-1 means use a value based on \"shared_buffers\" and the maximum number of processes."),
		.vartype = PGC_INT,
		._int = {
			.variable = &io_max_concurrency,
			.boot_val = -1,
			.min = -1,
			.max = 1024,
			.check_hook = check_io_max_concurrency,
		},
	},

	{
		.name = "io_max_workers",
		.context = PGC_SIGHUP,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_max_workers" short description */
		.short_desc = gettext_noop("Maximum number of I/O worker processes, for io_method=worker."),
		.vartype = PGC_INT,
		._int = {
			.variable = &io_max_workers,
			.boot_val = 8,
			.min = 1,
			.max = MAX_IO_WORKERS,
		},
	},

	{
		.name = "io_method",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_method" short description */
		.short_desc = gettext_noop("Selects the method for executing asynchronous I/O."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &io_method,
			.boot_val = DEFAULT_IO_METHOD,
			.options = io_method_options,
			.assign_hook = assign_io_method,
		},
	},

	{
		.name = "io_min_workers",
		.context = PGC_SIGHUP,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_min_workers" short description */
		.short_desc = gettext_noop("Minimum number of I/O worker processes, for io_method=worker."),
		.vartype = PGC_INT,
		._int = {
			.variable = &io_min_workers,
			.boot_val = 2,
			.min = 1,
			.max = MAX_IO_WORKERS,
		},
	},

	{
		.name = "io_worker_idle_timeout",
		.context = PGC_SIGHUP,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_worker_idle_timeout" short description */
		.short_desc = gettext_noop("Maximum time before idle I/O worker processes time out, for io_method=worker."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &io_worker_idle_timeout,
			.boot_val = 60000,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "io_worker_launch_interval",
		.context = PGC_SIGHUP,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "io_worker_launch_interval" short description */
		.short_desc = gettext_noop("Minimum time before launching a new I/O worker process, for io_method=worker."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &io_worker_launch_interval,
			.boot_val = 100,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "is_superuser",
		.context = PGC_INTERNAL,
		.group = UNGROUPED,
		/* translator: GUC parameter "is_superuser" short description */
		.short_desc = gettext_noop("Shows whether the current user is a superuser."),
		.flags = GUC_REPORT | GUC_NO_SHOW_ALL | GUC_NO_RESET_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_ALLOW_IN_PARALLEL,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &current_role_is_superuser,
			.boot_val = false,
		},
	},

	{
		.name = "jit",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "jit" short description */
		.short_desc = gettext_noop("Allow JIT compilation."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &jit_enabled,
			.boot_val = false,
		},
	},

	{
		.name = "jit_above_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "jit_above_cost" short description */
		.short_desc = gettext_noop("Perform JIT compilation if query is more expensive."),
		/* translator: GUC parameter "jit_above_cost" long description */
		.long_desc = gettext_noop("-1 disables JIT compilation."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &jit_above_cost,
			.boot_val = 100000,
			.min = -1,
			.max = DBL_MAX,
		},
	},

	{
		.name = "jit_debugging_support",
		.context = PGC_SU_BACKEND,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "jit_debugging_support" short description */
		.short_desc = gettext_noop("Register JIT-compiled functions with debugger."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &jit_debugging_support,
			.boot_val = false,
		},
	},

	{
		.name = "jit_dump_bitcode",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "jit_dump_bitcode" short description */
		.short_desc = gettext_noop("Write out LLVM bitcode to facilitate JIT debugging."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &jit_dump_bitcode,
			.boot_val = false,
		},
	},

	{
		.name = "jit_expressions",
		.context = PGC_USERSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "jit_expressions" short description */
		.short_desc = gettext_noop("Allow JIT compilation of expressions."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &jit_expressions,
			.boot_val = true,
		},
	},

	{
		.name = "jit_inline_above_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "jit_inline_above_cost" short description */
		.short_desc = gettext_noop("Perform JIT inlining if query is more expensive."),
		/* translator: GUC parameter "jit_inline_above_cost" long description */
		.long_desc = gettext_noop("-1 disables inlining."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &jit_inline_above_cost,
			.boot_val = 500000,
			.min = -1,
			.max = DBL_MAX,
		},
	},

	{
		.name = "jit_optimize_above_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "jit_optimize_above_cost" short description */
		.short_desc = gettext_noop("Optimize JIT-compiled functions if query is more expensive."),
		/* translator: GUC parameter "jit_optimize_above_cost" long description */
		.long_desc = gettext_noop("-1 disables optimization."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &jit_optimize_above_cost,
			.boot_val = 500000,
			.min = -1,
			.max = DBL_MAX,
		},
	},

	{
		.name = "jit_profiling_support",
		.context = PGC_SU_BACKEND,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "jit_profiling_support" short description */
		.short_desc = gettext_noop("Register JIT-compiled functions with perf profiler."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &jit_profiling_support,
			.boot_val = false,
		},
	},

	{
		.name = "jit_provider",
		.context = PGC_POSTMASTER,
		.group = CLIENT_CONN_PRELOAD,
		/* translator: GUC parameter "jit_provider" short description */
		.short_desc = gettext_noop("JIT provider to use."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &jit_provider,
			.boot_val = "llvmjit",
		},
	},

	{
		.name = "jit_tuple_deforming",
		.context = PGC_USERSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "jit_tuple_deforming" short description */
		.short_desc = gettext_noop("Allow JIT compilation of tuple deforming."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &jit_tuple_deforming,
			.boot_val = true,
		},
	},

	{
		.name = "join_collapse_limit",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "join_collapse_limit" short description */
		.short_desc = gettext_noop("Sets the FROM-list size beyond which JOIN constructs are not flattened."),
		/* translator: GUC parameter "join_collapse_limit" long description */
		.long_desc = gettext_noop("The planner will flatten explicit JOIN constructs into lists of FROM items whenever a list of no more than this many items would result."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &join_collapse_limit,
			.boot_val = 8,
			.min = 1,
			.max = INT_MAX,
		},
	},

	{
		.name = "krb_caseins_users",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "krb_caseins_users" short description */
		.short_desc = gettext_noop("Sets whether Kerberos and GSSAPI user names should be treated as case-insensitive."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &pg_krb_caseins_users,
			.boot_val = false,
		},
	},

	{
		.name = "krb_server_keyfile",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "krb_server_keyfile" short description */
		.short_desc = gettext_noop("Sets the location of the Kerberos server key file."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &pg_krb_server_keyfile,
			.boot_val = PG_KRB_SRVTAB,
		},
	},

	{
		.name = "lc_messages",
		.context = PGC_SUSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "lc_messages" short description */
		.short_desc = gettext_noop("Sets the language in which messages are displayed."),
		/* translator: GUC parameter "lc_messages" long description */
		.long_desc = gettext_noop("An empty string means use the operating system setting."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &locale_messages,
			.boot_val = "",
			.check_hook = check_locale_messages,
			.assign_hook = assign_locale_messages,
		},
	},

	{
		.name = "lc_monetary",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "lc_monetary" short description */
		.short_desc = gettext_noop("Sets the locale for formatting monetary amounts."),
		/* translator: GUC parameter "lc_monetary" long description */
		.long_desc = gettext_noop("An empty string means use the operating system setting."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &locale_monetary,
			.boot_val = "C",
			.check_hook = check_locale_monetary,
			.assign_hook = assign_locale_monetary,
		},
	},

	{
		.name = "lc_numeric",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "lc_numeric" short description */
		.short_desc = gettext_noop("Sets the locale for formatting numbers."),
		/* translator: GUC parameter "lc_numeric" long description */
		.long_desc = gettext_noop("An empty string means use the operating system setting."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &locale_numeric,
			.boot_val = "C",
			.check_hook = check_locale_numeric,
			.assign_hook = assign_locale_numeric,
		},
	},

	{
		.name = "lc_time",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "lc_time" short description */
		.short_desc = gettext_noop("Sets the locale for formatting date and time values."),
		/* translator: GUC parameter "lc_time" long description */
		.long_desc = gettext_noop("An empty string means use the operating system setting."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &locale_time,
			.boot_val = "C",
			.check_hook = check_locale_time,
			.assign_hook = assign_locale_time,
		},
	},

	{
		.name = "listen_addresses",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "listen_addresses" short description */
		.short_desc = gettext_noop("Sets the host name or IP address(es) to listen to."),
		.flags = GUC_LIST_INPUT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &ListenAddresses,
			.boot_val = "localhost",
		},
	},

	{
		.name = "lo_compat_privileges",
		.context = PGC_SUSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "lo_compat_privileges" short description */
		.short_desc = gettext_noop("Enables backward compatibility mode for privilege checks on large objects."),
		/* translator: GUC parameter "lo_compat_privileges" long description */
		.long_desc = gettext_noop("Skips privilege checks when reading or modifying large objects, for compatibility with PostgreSQL releases prior to 9.0."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &lo_compat_privileges,
			.boot_val = false,
		},
	},

	{
		.name = "local_preload_libraries",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_PRELOAD,
		/* translator: GUC parameter "local_preload_libraries" short description */
		.short_desc = gettext_noop("Lists unprivileged shared libraries to preload into each backend."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &local_preload_libraries_string,
			.boot_val = "",
		},
	},

	{
		.name = "lock_timeout",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "lock_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum allowed duration of any wait for a lock."),
		/* translator: GUC parameter "lock_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &LockTimeout,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "log_autoanalyze_min_duration",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_autoanalyze_min_duration" short description */
		.short_desc = gettext_noop("Sets the minimum execution time above which analyze actions by autovacuum will be logged."),
		/* translator: GUC parameter "log_autoanalyze_min_duration" long description */
		.long_desc = gettext_noop("-1 disables logging analyze actions by autovacuum. 0 means log all analyze actions by autovacuum."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &Log_autoanalyze_min_duration,
			.boot_val = 600000,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "log_autovacuum_min_duration",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_autovacuum_min_duration" short description */
		.short_desc = gettext_noop("Sets the minimum execution time above which vacuum actions by autovacuum will be logged."),
		/* translator: GUC parameter "log_autovacuum_min_duration" long description */
		.long_desc = gettext_noop("-1 disables logging vacuum actions by autovacuum. 0 means log all vacuum actions by autovacuum."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &Log_autovacuum_min_duration,
			.boot_val = 600000,
			.min = -1,
			.max = INT_MAX,
		},
	},

#ifdef BTREE_BUILD_STATS
	{
		.name = "log_btree_build_stats",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "log_btree_build_stats" short description */
		.short_desc = gettext_noop("Logs system resource usage statistics (memory and CPU) on various B-tree operations."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_btree_build_stats,
			.boot_val = false,
		},
	},
#endif

	{
		.name = "log_checkpoints",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_checkpoints" short description */
		.short_desc = gettext_noop("Logs each checkpoint."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_checkpoints,
			.boot_val = true,
		},
	},

	{
		.name = "log_connections",
		.context = PGC_SU_BACKEND,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_connections" short description */
		.short_desc = gettext_noop("Logs specified aspects of connection establishment and setup."),
		.flags = GUC_LIST_INPUT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &log_connections_string,
			.boot_val = "",
			.check_hook = check_log_connections,
			.assign_hook = assign_log_connections,
		},
	},

	{
		.name = "log_destination",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "log_destination" short description */
		.short_desc = gettext_noop("Sets the destination for server log output."),
		/* translator: GUC parameter "log_destination" long description */
		.long_desc = gettext_noop("Valid values are combinations of \"stderr\", \"syslog\", \"csvlog\", \"jsonlog\", and \"eventlog\", depending on the platform."),
		.flags = GUC_LIST_INPUT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &Log_destination_string,
			.boot_val = "stderr",
			.check_hook = check_log_destination,
			.assign_hook = assign_log_destination,
		},
	},

	{
		.name = "log_directory",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "log_directory" short description */
		.short_desc = gettext_noop("Sets the destination directory for log files."),
		/* translator: GUC parameter "log_directory" long description */
		.long_desc = gettext_noop("Can be specified as relative to the data directory or as absolute path."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &Log_directory,
			.boot_val = "log",
			.check_hook = check_canonical_path,
		},
	},

	{
		.name = "log_disconnections",
		.context = PGC_SU_BACKEND,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_disconnections" short description */
		.short_desc = gettext_noop("Logs end of a session, including duration."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Log_disconnections,
			.boot_val = false,
		},
	},

	{
		.name = "log_duration",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_duration" short description */
		.short_desc = gettext_noop("Logs the duration of each completed SQL statement."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_duration,
			.boot_val = false,
		},
	},

	{
		.name = "log_error_verbosity",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_error_verbosity" short description */
		.short_desc = gettext_noop("Sets the verbosity of logged messages."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &Log_error_verbosity,
			.boot_val = PGERROR_DEFAULT,
			.options = log_error_verbosity_options,
		},
	},

	{
		.name = "log_executor_stats",
		.context = PGC_SUSET,
		.group = STATS_MONITORING,
		/* translator: GUC parameter "log_executor_stats" short description */
		.short_desc = gettext_noop("Writes executor performance statistics to the server log."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_executor_stats,
			.boot_val = false,
			.check_hook = check_stage_log_stats,
		},
	},

	{
		.name = "log_file_mode",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "log_file_mode" short description */
		.short_desc = gettext_noop("Sets the file permissions for log files."),
		/* translator: GUC parameter "log_file_mode" long description */
		.long_desc = gettext_noop("The parameter value is expected to be a numeric mode specification in the form accepted by the chmod and umask system calls. (To use the customary octal format the number must start with a 0 (zero).)"),
		.flags = GUC_SHOW_IN_OCTAL,
		.vartype = PGC_INT,
		._int = {
			.variable = &Log_file_mode,
			.boot_val = 0600,
			.min = 0000,
			.max = 0777,
		},
	},

	{
		.name = "log_filename",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "log_filename" short description */
		.short_desc = gettext_noop("Sets the file name pattern for log files."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &Log_filename,
			.boot_val = "postgresql-%Y-%m-%d_%H%M%S.log",
		},
	},

	{
		.name = "log_hostname",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_hostname" short description */
		.short_desc = gettext_noop("Logs the host name in the connection logs."),
		/* translator: GUC parameter "log_hostname" long description */
		.long_desc = gettext_noop("By default, connection logs only show the IP address of the connecting host. If you want them to show the host name you can turn this on, but depending on your host name resolution setup it might impose a non-negligible performance penalty."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_hostname,
			.boot_val = false,
		},
	},

	{
		.name = "log_line_prefix",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_line_prefix" short description */
		.short_desc = gettext_noop("Controls information prefixed to each log line."),
		/* translator: GUC parameter "log_line_prefix" long description */
		.long_desc = gettext_noop("An empty string means no prefix."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &Log_line_prefix,
			.boot_val = "%m [%p] ",
		},
	},

	{
		.name = "log_lock_failures",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_lock_failures" short description */
		.short_desc = gettext_noop("Logs lock failures."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_lock_failures,
			.boot_val = false,
		},
	},

	{
		.name = "log_lock_waits",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_lock_waits" short description */
		.short_desc = gettext_noop("Logs long lock waits."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_lock_waits,
			.boot_val = true,
		},
	},

	{
		.name = "log_min_duration_sample",
		.context = PGC_SUSET,
		.group = LOGGING_WHEN,
		/* translator: GUC parameter "log_min_duration_sample" short description */
		.short_desc = gettext_noop("Sets the minimum execution time above which a sample of statements will be logged. Sampling is determined by \"log_statement_sample_rate\"."),
		/* translator: GUC parameter "log_min_duration_sample" long description */
		.long_desc = gettext_noop("-1 disables sampling. 0 means sample all statements."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &log_min_duration_sample,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "log_min_duration_statement",
		.context = PGC_SUSET,
		.group = LOGGING_WHEN,
		/* translator: GUC parameter "log_min_duration_statement" short description */
		.short_desc = gettext_noop("Sets the minimum execution time above which all statements will be logged."),
		/* translator: GUC parameter "log_min_duration_statement" long description */
		.long_desc = gettext_noop("-1 disables logging statement durations. 0 means log all statement durations."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &log_min_duration_statement,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "log_min_error_statement",
		.context = PGC_SUSET,
		.group = LOGGING_WHEN,
		/* translator: GUC parameter "log_min_error_statement" short description */
		.short_desc = gettext_noop("Causes all statements generating error at or above this level to be logged."),
		/* translator: GUC parameter "log_min_error_statement" long description */
		.long_desc = gettext_noop("Each level includes all the levels that follow it. The later the level, the fewer messages are sent."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &log_min_error_statement,
			.boot_val = ERROR,
			.options = server_message_level_options,
		},
	},

	{
		.name = "log_min_messages",
		.context = PGC_SUSET,
		.group = LOGGING_WHEN,
		/* translator: GUC parameter "log_min_messages" short description */
		.short_desc = gettext_noop("Sets the message levels that are logged."),
		/* translator: GUC parameter "log_min_messages" long description */
		.long_desc = gettext_noop("Each level includes all the levels that follow it. The later the level, the fewer messages are sent."),
		.flags = GUC_LIST_INPUT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &log_min_messages_string,
			.boot_val = "warning",
			.check_hook = check_log_min_messages,
			.assign_hook = assign_log_min_messages,
		},
	},

	{
		.name = "log_parameter_max_length",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_parameter_max_length" short description */
		.short_desc = gettext_noop("Sets the maximum length in bytes of data logged for bind parameter values when logging statements."),
		/* translator: GUC parameter "log_parameter_max_length" long description */
		.long_desc = gettext_noop("-1 means log values in full."),
		.flags = GUC_UNIT_BYTE,
		.vartype = PGC_INT,
		._int = {
			.variable = &log_parameter_max_length,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX / 2,
		},
	},

	{
		.name = "log_parameter_max_length_on_error",
		.context = PGC_USERSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_parameter_max_length_on_error" short description */
		.short_desc = gettext_noop("Sets the maximum length in bytes of data logged for bind parameter values when logging statements, on error."),
		/* translator: GUC parameter "log_parameter_max_length_on_error" long description */
		.long_desc = gettext_noop("-1 means log values in full."),
		.flags = GUC_UNIT_BYTE,
		.vartype = PGC_INT,
		._int = {
			.variable = &log_parameter_max_length_on_error,
			.boot_val = 0,
			.min = -1,
			.max = INT_MAX / 2,
		},
	},

	{
		.name = "log_parser_stats",
		.context = PGC_SUSET,
		.group = STATS_MONITORING,
		/* translator: GUC parameter "log_parser_stats" short description */
		.short_desc = gettext_noop("Writes parser performance statistics to the server log."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_parser_stats,
			.boot_val = false,
			.check_hook = check_stage_log_stats,
		},
	},

	{
		.name = "log_planner_stats",
		.context = PGC_SUSET,
		.group = STATS_MONITORING,
		/* translator: GUC parameter "log_planner_stats" short description */
		.short_desc = gettext_noop("Writes planner performance statistics to the server log."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_planner_stats,
			.boot_val = false,
			.check_hook = check_stage_log_stats,
		},
	},

	{
		.name = "log_recovery_conflict_waits",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_recovery_conflict_waits" short description */
		.short_desc = gettext_noop("Logs standby recovery conflict waits."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_recovery_conflict_waits,
			.boot_val = false,
		},
	},

	{
		.name = "log_replication_commands",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_replication_commands" short description */
		.short_desc = gettext_noop("Logs each replication command."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_replication_commands,
			.boot_val = false,
		},
	},

	{
		.name = "log_rotation_age",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "log_rotation_age" short description */
		.short_desc = gettext_noop("Sets the amount of time to wait before forcing log file rotation."),
		/* translator: GUC parameter "log_rotation_age" long description */
		.long_desc = gettext_noop("0 disables time-based creation of new log files."),
		.flags = GUC_UNIT_MIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &Log_RotationAge,
			.boot_val = HOURS_PER_DAY * MINS_PER_HOUR,
			.min = 0,
			.max = INT_MAX / SECS_PER_MINUTE,
		},
	},

	{
		.name = "log_rotation_size",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "log_rotation_size" short description */
		.short_desc = gettext_noop("Sets the maximum size a log file can reach before being rotated."),
		/* translator: GUC parameter "log_rotation_size" long description */
		.long_desc = gettext_noop("0 disables size-based creation of new log files."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &Log_RotationSize,
			.boot_val = 10 * 1024,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "log_startup_progress_interval",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHEN,
		/* translator: GUC parameter "log_startup_progress_interval" short description */
		.short_desc = gettext_noop("Time between progress updates for long-running startup operations."),
		/* translator: GUC parameter "log_startup_progress_interval" long description */
		.long_desc = gettext_noop("0 disables progress updates."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &log_startup_progress_interval,
			.boot_val = 10000,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "log_statement",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_statement" short description */
		.short_desc = gettext_noop("Sets the type of statements logged."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &log_statement,
			.boot_val = LOGSTMT_NONE,
			.options = log_statement_options,
		},
	},

	{
		.name = "log_statement_max_length",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_statement_max_length" short description */
		.short_desc = gettext_noop("Sets the maximum length in bytes of logged statement text."),
		/* translator: GUC parameter "log_statement_max_length" long description */
		.long_desc = gettext_noop("-1 means log statements in full. 0 means log only an ellipsis."),
		.flags = GUC_UNIT_BYTE,
		.vartype = PGC_INT,
		._int = {
			.variable = &log_statement_max_length,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX / 2,
		},
	},

	{
		.name = "log_statement_sample_rate",
		.context = PGC_SUSET,
		.group = LOGGING_WHEN,
		/* translator: GUC parameter "log_statement_sample_rate" short description */
		.short_desc = gettext_noop("Fraction of statements exceeding \"log_min_duration_sample\" to be logged."),
		/* translator: GUC parameter "log_statement_sample_rate" long description */
		.long_desc = gettext_noop("Use a value between 0.0 (never log) and 1.0 (always log)."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &log_statement_sample_rate,
			.boot_val = 1.0,
			.min = 0.0,
			.max = 1.0,
		},
	},

	{
		.name = "log_statement_stats",
		.context = PGC_SUSET,
		.group = STATS_MONITORING,
		/* translator: GUC parameter "log_statement_stats" short description */
		.short_desc = gettext_noop("Writes cumulative performance statistics to the server log."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &log_statement_stats,
			.boot_val = false,
			.check_hook = check_log_stats,
		},
	},

	{
		.name = "log_temp_files",
		.context = PGC_SUSET,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_temp_files" short description */
		.short_desc = gettext_noop("Log the use of temporary files larger than this number of kilobytes."),
		/* translator: GUC parameter "log_temp_files" long description */
		.long_desc = gettext_noop("-1 disables logging temporary files. 0 means log all temporary files."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &log_temp_files,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "log_timezone",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHAT,
		/* translator: GUC parameter "log_timezone" short description */
		.short_desc = gettext_noop("Sets the time zone to use in log messages."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &log_timezone_string,
			.boot_val = "GMT",
			.check_hook = check_log_timezone,
			.assign_hook = assign_log_timezone,
			.show_hook = show_log_timezone,
		},
	},

	{
		.name = "log_transaction_sample_rate",
		.context = PGC_SUSET,
		.group = LOGGING_WHEN,
		/* translator: GUC parameter "log_transaction_sample_rate" short description */
		.short_desc = gettext_noop("Sets the fraction of transactions from which to log all statements."),
		/* translator: GUC parameter "log_transaction_sample_rate" long description */
		.long_desc = gettext_noop("Use a value between 0.0 (never log) and 1.0 (log all statements for all transactions)."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &log_xact_sample_rate,
			.boot_val = 0.0,
			.min = 0.0,
			.max = 1.0,
		},
	},

	{
		.name = "log_truncate_on_rotation",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "log_truncate_on_rotation" short description */
		.short_desc = gettext_noop("Truncate existing log files of same name during log rotation."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Log_truncate_on_rotation,
			.boot_val = false,
		},
	},

	{
		.name = "logging_collector",
		.context = PGC_POSTMASTER,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "logging_collector" short description */
		.short_desc = gettext_noop("Start a subprocess to capture stderr, csvlog and/or jsonlog into log files."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Logging_collector,
			.boot_val = false,
		},
	},

	{
		.name = "logical_decoding_work_mem",
		.context = PGC_USERSET,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "logical_decoding_work_mem" short description */
		.short_desc = gettext_noop("Sets the maximum memory to be used for logical decoding."),
		/* translator: GUC parameter "logical_decoding_work_mem" long description */
		.long_desc = gettext_noop("This much memory can be used by each internal reorder buffer before spilling to disk."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &logical_decoding_work_mem,
			.boot_val = 65536,
			.min = 64,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "maintenance_io_concurrency",
		.context = PGC_USERSET,
		.group = RESOURCES_IO,
		/* translator: GUC parameter "maintenance_io_concurrency" short description */
		.short_desc = gettext_noop("A variant of \"effective_io_concurrency\" that is used for maintenance work."),
		/* translator: GUC parameter "maintenance_io_concurrency" long description */
		.long_desc = gettext_noop("0 disables simultaneous requests."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &maintenance_io_concurrency,
			.boot_val = DEFAULT_MAINTENANCE_IO_CONCURRENCY,
			.min = 0,
			.max = MAX_IO_CONCURRENCY,
			.assign_hook = assign_maintenance_io_concurrency,
		},
	},

	{
		.name = "maintenance_work_mem",
		.context = PGC_USERSET,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "maintenance_work_mem" short description */
		.short_desc = gettext_noop("Sets the maximum memory to be used for maintenance operations."),
		/* translator: GUC parameter "maintenance_work_mem" long description */
		.long_desc = gettext_noop("This includes operations such as VACUUM and CREATE INDEX."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &maintenance_work_mem,
			.boot_val = 65536,
			.min = 64,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "max_active_replication_origins",
		.context = PGC_POSTMASTER,
		.group = REPLICATION_SUBSCRIBERS,
		/* translator: GUC parameter "max_active_replication_origins" short description */
		.short_desc = gettext_noop("Sets the maximum number of active replication origins."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_active_replication_origins,
			.boot_val = 10,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "max_connections",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "max_connections" short description */
		.short_desc = gettext_noop("Sets the maximum number of concurrent connections."),
		.vartype = PGC_INT,
		._int = {
			.variable = &MaxConnections,
			.boot_val = 100,
			.min = 1,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "max_files_per_process",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_KERNEL,
		/* translator: GUC parameter "max_files_per_process" short description */
		.short_desc = gettext_noop("Sets the maximum number of files each server process is allowed to open simultaneously."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_files_per_process,
			.boot_val = 1000,
			.min = 64,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_function_args",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "max_function_args" short description */
		.short_desc = gettext_noop("Shows the maximum number of function arguments."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_function_args,
			.boot_val = FUNC_MAX_ARGS,
			.min = FUNC_MAX_ARGS,
			.max = FUNC_MAX_ARGS,
		},
	},

	{
		.name = "max_identifier_length",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "max_identifier_length" short description */
		.short_desc = gettext_noop("Shows the maximum identifier length."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_identifier_length,
			.boot_val = NAMEDATALEN - 1,
			.min = NAMEDATALEN - 1,
			.max = NAMEDATALEN - 1,
		},
	},

	{
		.name = "max_index_keys",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "max_index_keys" short description */
		.short_desc = gettext_noop("Shows the maximum number of index keys."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_index_keys,
			.boot_val = INDEX_MAX_KEYS,
			.min = INDEX_MAX_KEYS,
			.max = INDEX_MAX_KEYS,
		},
	},

	{
		.name = "max_locks_per_transaction",
		.context = PGC_POSTMASTER,
		.group = LOCK_MANAGEMENT,
		/* translator: GUC parameter "max_locks_per_transaction" short description */
		.short_desc = gettext_noop("Sets the maximum number of locks per transaction."),
		/* translator: GUC parameter "max_locks_per_transaction" long description */
		.long_desc = gettext_noop("The shared lock table is sized on the assumption that at most \"max_locks_per_transaction\" objects per server process or prepared transaction will need to be locked at any one time."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_locks_per_xact,
			.boot_val = 128,
			.min = 10,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_logical_replication_workers",
		.context = PGC_POSTMASTER,
		.group = REPLICATION_SUBSCRIBERS,
		/* translator: GUC parameter "max_logical_replication_workers" short description */
		.short_desc = gettext_noop("Maximum number of logical replication worker processes."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_logical_replication_workers,
			.boot_val = 4,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "max_notify_queue_pages",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_DISK,
		/* translator: GUC parameter "max_notify_queue_pages" short description */
		.short_desc = gettext_noop("Sets the maximum number of allocated pages for NOTIFY / LISTEN queue."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_notify_queue_pages,
			.boot_val = 1048576,
			.min = 64,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_parallel_apply_workers_per_subscription",
		.context = PGC_SIGHUP,
		.group = REPLICATION_SUBSCRIBERS,
		/* translator: GUC parameter "max_parallel_apply_workers_per_subscription" short description */
		.short_desc = gettext_noop("Maximum number of parallel apply workers per subscription."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_parallel_apply_workers_per_subscription,
			.boot_val = 2,
			.min = 0,
			.max = MAX_PARALLEL_WORKER_LIMIT,
		},
	},

	{
		.name = "max_parallel_maintenance_workers",
		.context = PGC_USERSET,
		.group = RESOURCES_WORKER_PROCESSES,
		/* translator: GUC parameter "max_parallel_maintenance_workers" short description */
		.short_desc = gettext_noop("Sets the maximum number of parallel processes per maintenance operation."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_parallel_maintenance_workers,
			.boot_val = 2,
			.min = 0,
			.max = MAX_PARALLEL_WORKER_LIMIT,
		},
	},

	{
		.name = "max_parallel_workers",
		.context = PGC_USERSET,
		.group = RESOURCES_WORKER_PROCESSES,
		/* translator: GUC parameter "max_parallel_workers" short description */
		.short_desc = gettext_noop("Sets the maximum number of parallel workers that can be active at one time."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_parallel_workers,
			.boot_val = 8,
			.min = 0,
			.max = MAX_PARALLEL_WORKER_LIMIT,
		},
	},

	{
		.name = "max_parallel_workers_per_gather",
		.context = PGC_USERSET,
		.group = RESOURCES_WORKER_PROCESSES,
		/* translator: GUC parameter "max_parallel_workers_per_gather" short description */
		.short_desc = gettext_noop("Sets the maximum number of parallel processes per executor node."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_parallel_workers_per_gather,
			.boot_val = 2,
			.min = 0,
			.max = MAX_PARALLEL_WORKER_LIMIT,
		},
	},

	{
		.name = "max_pred_locks_per_page",
		.context = PGC_SIGHUP,
		.group = LOCK_MANAGEMENT,
		/* translator: GUC parameter "max_pred_locks_per_page" short description */
		.short_desc = gettext_noop("Sets the maximum number of predicate-locked tuples per page."),
		/* translator: GUC parameter "max_pred_locks_per_page" long description */
		.long_desc = gettext_noop("If more than this number of tuples on the same page are locked by a connection, those locks are replaced by a page-level lock."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_predicate_locks_per_page,
			.boot_val = 2,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_pred_locks_per_relation",
		.context = PGC_SIGHUP,
		.group = LOCK_MANAGEMENT,
		/* translator: GUC parameter "max_pred_locks_per_relation" short description */
		.short_desc = gettext_noop("Sets the maximum number of predicate-locked pages and tuples per relation."),
		/* translator: GUC parameter "max_pred_locks_per_relation" long description */
		.long_desc = gettext_noop("If more than this total of pages and tuples in the same relation are locked by a connection, those locks are replaced by a relation-level lock."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_predicate_locks_per_relation,
			.boot_val = -2,
			.min = INT_MIN,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_pred_locks_per_transaction",
		.context = PGC_POSTMASTER,
		.group = LOCK_MANAGEMENT,
		/* translator: GUC parameter "max_pred_locks_per_transaction" short description */
		.short_desc = gettext_noop("Sets the maximum number of predicate locks per transaction."),
		/* translator: GUC parameter "max_pred_locks_per_transaction" long description */
		.long_desc = gettext_noop("The shared predicate lock table is sized on the assumption that at most \"max_pred_locks_per_transaction\" objects per server process or prepared transaction will need to be locked at any one time."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_predicate_locks_per_xact,
			.boot_val = 64,
			.min = 10,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_prepared_transactions",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "max_prepared_transactions" short description */
		.short_desc = gettext_noop("Sets the maximum number of simultaneously prepared transactions."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_prepared_xacts,
			.boot_val = 0,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "max_repack_replication_slots",
		.context = PGC_POSTMASTER,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "max_repack_replication_slots" short description */
		.short_desc = gettext_noop("Sets the maximum number of replication slots for use by REPACK."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_repack_replication_slots,
			.boot_val = 5,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "max_replication_slots",
		.context = PGC_POSTMASTER,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "max_replication_slots" short description */
		.short_desc = gettext_noop("Sets the maximum number of simultaneously defined replication slots."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_replication_slots,
			.boot_val = 10,
			.min = 0,
			.max = MAX_BACKENDS /* XXX? */,
		},
	},

	{
		.name = "max_slot_wal_keep_size",
		.context = PGC_SIGHUP,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "max_slot_wal_keep_size" short description */
		.short_desc = gettext_noop("Sets the maximum WAL size that can be reserved by replication slots."),
		/* translator: GUC parameter "max_slot_wal_keep_size" long description */
		.long_desc = gettext_noop("Replication slots will be marked as failed, and segments released for deletion or recycling, if this much space is occupied by WAL on disk. -1 means no maximum."),
		.flags = GUC_UNIT_MB,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_slot_wal_keep_size_mb,
			.boot_val = -1,
			.min = -1,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "max_stack_depth",
		.context = PGC_SUSET,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "max_stack_depth" short description */
		.short_desc = gettext_noop("Sets the maximum stack depth, in kilobytes."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_stack_depth,
			.boot_val = 100,
			.min = 100,
			.max = MAX_KILOBYTES,
			.check_hook = check_max_stack_depth,
			.assign_hook = assign_max_stack_depth,
		},
	},

	{
		.name = "max_standby_archive_delay",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "max_standby_archive_delay" short description */
		.short_desc = gettext_noop("Sets the maximum delay before canceling queries when a hot standby server is processing archived WAL data."),
		/* translator: GUC parameter "max_standby_archive_delay" long description */
		.long_desc = gettext_noop("-1 means wait forever."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_standby_archive_delay,
			.boot_val = 30 * 1000,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_standby_streaming_delay",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "max_standby_streaming_delay" short description */
		.short_desc = gettext_noop("Sets the maximum delay before canceling queries when a hot standby server is processing streamed WAL data."),
		/* translator: GUC parameter "max_standby_streaming_delay" long description */
		.long_desc = gettext_noop("-1 means wait forever."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_standby_streaming_delay,
			.boot_val = 30 * 1000,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "max_sync_workers_per_subscription",
		.context = PGC_SIGHUP,
		.group = REPLICATION_SUBSCRIBERS,
		/* translator: GUC parameter "max_sync_workers_per_subscription" short description */
		.short_desc = gettext_noop("Maximum number of workers per subscription for synchronizing tables and sequences."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_sync_workers_per_subscription,
			.boot_val = 2,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "max_wal_senders",
		.context = PGC_POSTMASTER,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "max_wal_senders" short description */
		.short_desc = gettext_noop("Sets the maximum number of simultaneously running WAL sender processes."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_wal_senders,
			.boot_val = 10,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "max_wal_size",
		.context = PGC_SIGHUP,
		.group = WAL_CHECKPOINTS,
		/* translator: GUC parameter "max_wal_size" short description */
		.short_desc = gettext_noop("Sets the WAL size that triggers a checkpoint."),
		.flags = GUC_UNIT_MB,
		.vartype = PGC_INT,
		._int = {
			.variable = &max_wal_size_mb,
			.boot_val = DEFAULT_MAX_WAL_SEGS * (DEFAULT_XLOG_SEG_SIZE / (1024 * 1024)),
			.min = 2,
			.max = MAX_KILOBYTES,
			.assign_hook = assign_max_wal_size,
		},
	},

	{
		.name = "max_worker_processes",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_WORKER_PROCESSES,
		/* translator: GUC parameter "max_worker_processes" short description */
		.short_desc = gettext_noop("Maximum number of concurrent worker processes."),
		.vartype = PGC_INT,
		._int = {
			.variable = &max_worker_processes,
			.boot_val = 8,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "md5_password_warnings",
		.context = PGC_USERSET,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "md5_password_warnings" short description */
		.short_desc = gettext_noop("Enables deprecation warnings for MD5 passwords."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &md5_password_warnings,
			.boot_val = true,
		},
	},

	{
		.name = "min_dynamic_shared_memory",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "min_dynamic_shared_memory" short description */
		.short_desc = gettext_noop("Amount of dynamic shared memory reserved at startup."),
		.flags = GUC_UNIT_MB,
		.vartype = PGC_INT,
		._int = {
			.variable = &min_dynamic_shared_memory,
			.boot_val = 0,
			.min = 0,
			.max = (int) Min((size_t) INT_MAX, SIZE_MAX / (1024 * 1024)),
		},
	},

	{
		.name = "min_eager_agg_group_size",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "min_eager_agg_group_size" short description */
		.short_desc = gettext_noop("Sets the minimum average group size required to consider applying eager aggregation."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &min_eager_agg_group_size,
			.boot_val = 8.0,
			.min = 0.0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "min_parallel_index_scan_size",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "min_parallel_index_scan_size" short description */
		.short_desc = gettext_noop("Sets the minimum amount of index data for a parallel scan."),
		/* translator: GUC parameter "min_parallel_index_scan_size" long description */
		.long_desc = gettext_noop("If the planner estimates that it will read a number of index pages too small to reach this limit, a parallel scan will not be considered."),
		.flags = GUC_UNIT_BLOCKS | GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &min_parallel_index_scan_size,
			.boot_val = (512 * 1024) / BLCKSZ,
			.min = 0,
			.max = INT_MAX / 3,
		},
	},

	{
		.name = "min_parallel_table_scan_size",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "min_parallel_table_scan_size" short description */
		.short_desc = gettext_noop("Sets the minimum amount of table data for a parallel scan."),
		/* translator: GUC parameter "min_parallel_table_scan_size" long description */
		.long_desc = gettext_noop("If the planner estimates that it will read a number of table pages too small to reach this limit, a parallel scan will not be considered."),
		.flags = GUC_UNIT_BLOCKS | GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &min_parallel_table_scan_size,
			.boot_val = (8 * 1024 * 1024) / BLCKSZ,
			.min = 0,
			.max = INT_MAX / 3,
		},
	},

	{
		.name = "min_wal_size",
		.context = PGC_SIGHUP,
		.group = WAL_CHECKPOINTS,
		/* translator: GUC parameter "min_wal_size" short description */
		.short_desc = gettext_noop("Sets the minimum size to shrink the WAL to."),
		.flags = GUC_UNIT_MB,
		.vartype = PGC_INT,
		._int = {
			.variable = &min_wal_size_mb,
			.boot_val = DEFAULT_MIN_WAL_SEGS * (DEFAULT_XLOG_SEG_SIZE / (1024 * 1024)),
			.min = 2,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "multixact_member_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "multixact_member_buffers" short description */
		.short_desc = gettext_noop("Sets the size of the dedicated buffer pool used for the MultiXact member cache."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &multixact_member_buffers,
			.boot_val = 32,
			.min = 16,
			.max = SLRU_MAX_ALLOWED_BUFFERS,
			.check_hook = check_multixact_member_buffers,
		},
	},

	{
		.name = "multixact_offset_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "multixact_offset_buffers" short description */
		.short_desc = gettext_noop("Sets the size of the dedicated buffer pool used for the MultiXact offset cache."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &multixact_offset_buffers,
			.boot_val = 16,
			.min = 16,
			.max = SLRU_MAX_ALLOWED_BUFFERS,
			.check_hook = check_multixact_offset_buffers,
		},
	},

	{
		.name = "notify_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "notify_buffers" short description */
		.short_desc = gettext_noop("Sets the size of the dedicated buffer pool used for the LISTEN/NOTIFY message cache."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &notify_buffers,
			.boot_val = 16,
			.min = 16,
			.max = SLRU_MAX_ALLOWED_BUFFERS,
			.check_hook = check_notify_buffers,
		},
	},

	{
		.name = "num_os_semaphores",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "num_os_semaphores" short description */
		.short_desc = gettext_noop("Shows the number of semaphores required for the server."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_RUNTIME_COMPUTED,
		.vartype = PGC_INT,
		._int = {
			.variable = &num_os_semaphores,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "oauth_validator_libraries",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "oauth_validator_libraries" short description */
		.short_desc = gettext_noop("Lists libraries that may be called to validate OAuth v2 bearer tokens."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE | GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &oauth_validator_libraries_string,
			.boot_val = "",
		},
	},

#ifdef DEBUG_BOUNDED_SORT
	{
		.name = "optimize_bounded_sort",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_METHOD,
		/* translator: GUC parameter "optimize_bounded_sort" short description */
		.short_desc = gettext_noop("Enables bounded sorting using heap sort."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &optimize_bounded_sort,
			.boot_val = true,
		},
	},
#endif

	{
		.name = "output_plugin_libraries",
		.context = PGC_SUSET,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "output_plugin_libraries" short description */
		.short_desc = gettext_noop("Lists libraries that may be named as logical decoding output plugins."),
		/* translator: GUC parameter "output_plugin_libraries" long description */
		.long_desc = gettext_noop("Users with REPLICATION privileges may only use plugins in this list when creating logical replication slots."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE | GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &output_plugin_libraries_string,
			.boot_val = "pgoutput, test_decoding",
		},
	},

	{
		.name = "parallel_leader_participation",
		.context = PGC_USERSET,
		.group = RESOURCES_WORKER_PROCESSES,
		/* translator: GUC parameter "parallel_leader_participation" short description */
		.short_desc = gettext_noop("Controls whether Gather and Gather Merge also run subplans."),
		/* translator: GUC parameter "parallel_leader_participation" long description */
		.long_desc = gettext_noop("Should gather nodes also run subplans or just gather tuples?"),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &parallel_leader_participation,
			.boot_val = true,
		},
	},

	{
		.name = "parallel_setup_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "parallel_setup_cost" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the cost of starting up worker processes for parallel query."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &parallel_setup_cost,
			.boot_val = DEFAULT_PARALLEL_SETUP_COST,
			.min = 0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "parallel_tuple_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "parallel_tuple_cost" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the cost of passing each tuple (row) from worker to leader backend."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &parallel_tuple_cost,
			.boot_val = DEFAULT_PARALLEL_TUPLE_COST,
			.min = 0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "password_encryption",
		.context = PGC_USERSET,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "password_encryption" short description */
		.short_desc = gettext_noop("Chooses the algorithm for encrypting passwords."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &Password_encryption,
			.boot_val = PASSWORD_TYPE_SCRAM_SHA_256,
			.options = password_encryption_options,
		},
	},

	{
		.name = "password_expiration_warning_threshold",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "password_expiration_warning_threshold" short description */
		.short_desc = gettext_noop("Threshold for password expiration warnings."),
		/* translator: GUC parameter "password_expiration_warning_threshold" long description */
		.long_desc = gettext_noop("0 disables these warnings."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &password_expiration_warning_threshold,
			.boot_val = 604800,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "plan_cache_mode",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "plan_cache_mode" short description */
		.short_desc = gettext_noop("Controls the planner's selection of custom or generic plan."),
		/* translator: GUC parameter "plan_cache_mode" long description */
		.long_desc = gettext_noop("Prepared statements can have custom and generic plans, and the planner will attempt to choose which is better.  This can be set to override the default behavior."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &plan_cache_mode,
			.boot_val = PLAN_CACHE_MODE_AUTO,
			.options = plan_cache_mode_options,
		},
	},

	{
		.name = "port",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "port" short description */
		.short_desc = gettext_noop("Sets the TCP port the server listens on."),
		.vartype = PGC_INT,
		._int = {
			.variable = &PostPortNumber,
			.boot_val = DEF_PGPORT,
			.min = 1,
			.max = 65535,
		},
	},

	{
		.name = "post_auth_delay",
		.context = PGC_BACKEND,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "post_auth_delay" short description */
		.short_desc = gettext_noop("Sets the amount of time to wait after authentication on connection startup."),
		/* translator: GUC parameter "post_auth_delay" long description */
		.long_desc = gettext_noop("This allows attaching a debugger to the process."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &PostAuthDelay,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX / 1000000,
		},
	},

	{
		.name = "pre_auth_delay",
		.context = PGC_SIGHUP,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "pre_auth_delay" short description */
		.short_desc = gettext_noop("Sets the amount of time to wait before authentication on connection startup."),
		/* translator: GUC parameter "pre_auth_delay" long description */
		.long_desc = gettext_noop("This allows attaching a debugger to the process."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &PreAuthDelay,
			.boot_val = 0,
			.min = 0,
			.max = 60,
		},
	},

	{
		.name = "primary_conninfo",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "primary_conninfo" short description */
		.short_desc = gettext_noop("Sets the connection string to be used to connect to the sending server."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &PrimaryConnInfo,
			.boot_val = "",
		},
	},

	{
		.name = "primary_slot_name",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "primary_slot_name" short description */
		.short_desc = gettext_noop("Sets the name of the replication slot to use on the sending server."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &PrimarySlotName,
			.boot_val = "",
			.check_hook = check_primary_slot_name,
		},
	},

	{
		.name = "quote_all_identifiers",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "quote_all_identifiers" short description */
		.short_desc = gettext_noop("When generating SQL fragments, quote all identifiers."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &quote_all_identifiers,
			.boot_val = false,
		},
	},

	{
		.name = "random_page_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "random_page_cost" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the cost of a nonsequentially fetched disk page."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &random_page_cost,
			.boot_val = DEFAULT_RANDOM_PAGE_COST,
			.min = 0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "recovery_end_command",
		.context = PGC_SIGHUP,
		.group = WAL_ARCHIVE_RECOVERY,
		/* translator: GUC parameter "recovery_end_command" short description */
		.short_desc = gettext_noop("Sets the shell command that will be executed once at the end of recovery."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recoveryEndCommand,
			.boot_val = "",
		},
	},

	{
		.name = "recovery_init_sync_method",
		.context = PGC_SIGHUP,
		.group = ERROR_HANDLING_OPTIONS,
		/* translator: GUC parameter "recovery_init_sync_method" short description */
		.short_desc = gettext_noop("Sets the method for synchronizing the data directory before crash recovery."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &recovery_init_sync_method,
			.boot_val = DATA_DIR_SYNC_METHOD_FSYNC,
			.options = recovery_init_sync_method_options,
		},
	},

	{
		.name = "recovery_min_apply_delay",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "recovery_min_apply_delay" short description */
		.short_desc = gettext_noop("Sets the minimum delay for applying changes during recovery."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &recovery_min_apply_delay,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "recovery_prefetch",
		.context = PGC_SIGHUP,
		.group = WAL_RECOVERY,
		/* translator: GUC parameter "recovery_prefetch" short description */
		.short_desc = gettext_noop("Prefetch referenced blocks during recovery."),
		/* translator: GUC parameter "recovery_prefetch" long description */
		.long_desc = gettext_noop("Look ahead in the WAL to find references to uncached data."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &recovery_prefetch,
			.boot_val = RECOVERY_PREFETCH_TRY,
			.options = recovery_prefetch_options,
			.check_hook = check_recovery_prefetch,
			.assign_hook = assign_recovery_prefetch,
		},
	},

	{
		.name = "recovery_target",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target" short description */
		.short_desc = gettext_noop("Set to \"immediate\" to end recovery as soon as a consistent state is reached."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recovery_target_string,
			.boot_val = "",
			.check_hook = check_recovery_target,
		},
	},

	{
		.name = "recovery_target_action",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target_action" short description */
		.short_desc = gettext_noop("Sets the action to perform upon reaching the recovery target."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &recoveryTargetAction,
			.boot_val = RECOVERY_TARGET_ACTION_PAUSE,
			.options = recovery_target_action_options,
		},
	},

	{
		.name = "recovery_target_inclusive",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target_inclusive" short description */
		.short_desc = gettext_noop("Sets whether to include or exclude transaction with recovery target."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &recoveryTargetInclusive,
			.boot_val = true,
		},
	},

	{
		.name = "recovery_target_lsn",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target_lsn" short description */
		.short_desc = gettext_noop("Sets the LSN of the write-ahead log location up to which recovery will proceed."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recovery_target_lsn_string,
			.boot_val = "",
			.check_hook = check_recovery_target_lsn,
			.assign_hook = assign_recovery_target_lsn,
		},
	},

	{
		.name = "recovery_target_name",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target_name" short description */
		.short_desc = gettext_noop("Sets the named restore point up to which recovery will proceed."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recoveryTargetName,
			.boot_val = "",
			.check_hook = check_recovery_target_name,
		},
	},

	{
		.name = "recovery_target_time",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target_time" short description */
		.short_desc = gettext_noop("Sets the time stamp up to which recovery will proceed."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recovery_target_time_string,
			.boot_val = "",
			.check_hook = check_recovery_target_time,
		},
	},

	{
		.name = "recovery_target_timeline",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target_timeline" short description */
		.short_desc = gettext_noop("Specifies the timeline to recover into."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recovery_target_timeline_string,
			.boot_val = "latest",
			.check_hook = check_recovery_target_timeline,
			.assign_hook = assign_recovery_target_timeline,
		},
	},

	{
		.name = "recovery_target_xid",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY_TARGET,
		/* translator: GUC parameter "recovery_target_xid" short description */
		.short_desc = gettext_noop("Sets the transaction ID up to which recovery will proceed."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recovery_target_xid_string,
			.boot_val = "",
			.check_hook = check_recovery_target_xid,
			.assign_hook = assign_recovery_target_xid,
		},
	},

	{
		.name = "recursive_worktable_factor",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_OTHER,
		/* translator: GUC parameter "recursive_worktable_factor" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the average size of a recursive query's working table."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &recursive_worktable_factor,
			.boot_val = DEFAULT_RECURSIVE_WORKTABLE_FACTOR,
			.min = 0.001,
			.max = 1000000.0,
		},
	},

	{
		.name = "remove_temp_files_after_crash",
		.context = PGC_SIGHUP,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "remove_temp_files_after_crash" short description */
		.short_desc = gettext_noop("Remove temporary files after backend crash."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &remove_temp_files_after_crash,
			.boot_val = true,
		},
	},

	{
		.name = "reserved_connections",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "reserved_connections" short description */
		.short_desc = gettext_noop("Sets the number of connection slots reserved for roles with privileges of pg_use_reserved_connections."),
		.vartype = PGC_INT,
		._int = {
			.variable = &ReservedConnections,
			.boot_val = 0,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "restart_after_crash",
		.context = PGC_SIGHUP,
		.group = ERROR_HANDLING_OPTIONS,
		/* translator: GUC parameter "restart_after_crash" short description */
		.short_desc = gettext_noop("Reinitialize server after backend crash."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &restart_after_crash,
			.boot_val = true,
		},
	},

	{
		.name = "restore_command",
		.context = PGC_SIGHUP,
		.group = WAL_ARCHIVE_RECOVERY,
		/* translator: GUC parameter "restore_command" short description */
		.short_desc = gettext_noop("Sets the shell command that will be called to retrieve an archived WAL file."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &recoveryRestoreCommand,
			.boot_val = "",
		},
	},

	{
		.name = "restrict_nonsystem_relation_kind",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "restrict_nonsystem_relation_kind" short description */
		.short_desc = gettext_noop("Prohibits access to non-system relations of specified kinds."),
		.flags = GUC_LIST_INPUT | GUC_NOT_IN_SAMPLE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &restrict_nonsystem_relation_kind_string,
			.boot_val = "",
			.check_hook = check_restrict_nonsystem_relation_kind,
			.assign_hook = assign_restrict_nonsystem_relation_kind,
		},
	},

	{
		.name = "role",
		.context = PGC_USERSET,
		.group = UNGROUPED,
		/* translator: GUC parameter "role" short description */
		.short_desc = gettext_noop("Sets the current role."),
		.flags = GUC_IS_NAME | GUC_NO_SHOW_ALL | GUC_NO_RESET_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_NOT_WHILE_SEC_REST,
		.vartype = PGC_STRING,
		._string = {
			.variable = &role_string,
			.boot_val = "none",
			.check_hook = check_role,
			.assign_hook = assign_role,
			.show_hook = show_role,
		},
	},

	{
		.name = "row_security",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "row_security" short description */
		.short_desc = gettext_noop("Enables row security."),
		/* translator: GUC parameter "row_security" long description */
		.long_desc = gettext_noop("When enabled, row security will be applied to all users."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &row_security,
			.boot_val = true,
		},
	},

	{
		.name = "scram_iterations",
		.context = PGC_USERSET,
		.group = CONN_AUTH_AUTH,
		/* translator: GUC parameter "scram_iterations" short description */
		.short_desc = gettext_noop("Sets the iteration count for SCRAM secret generation."),
		.flags = GUC_REPORT,
		.vartype = PGC_INT,
		._int = {
			.variable = &scram_sha_256_iterations,
			.boot_val = SCRAM_SHA_256_DEFAULT_ITERATIONS,
			.min = 1,
			.max = INT_MAX,
		},
	},

	{
		.name = "search_path",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "search_path" short description */
		.short_desc = gettext_noop("Sets the schema search order for names that are not schema-qualified."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE | GUC_EXPLAIN | GUC_REPORT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &namespace_search_path,
			.boot_val = "\"$user\", public",
			.check_hook = check_search_path,
			.assign_hook = assign_search_path,
		},
	},

	{
		.name = "seed",
		.context = PGC_USERSET,
		.group = UNGROUPED,
		/* translator: GUC parameter "seed" short description */
		.short_desc = gettext_noop("Sets the seed for random-number generation."),
		.flags = GUC_NO_SHOW_ALL | GUC_NO_RESET | GUC_NO_RESET_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_REAL,
		._real = {
			.variable = &phony_random_seed,
			.boot_val = 0.0,
			.min = -1.0,
			.max = 1.0,
			.check_hook = check_random_seed,
			.assign_hook = assign_random_seed,
			.show_hook = show_random_seed,
		},
	},

	{
		.name = "segment_size",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "segment_size" short description */
		.short_desc = gettext_noop("Shows the number of pages per disk file."),
		.flags = GUC_UNIT_BLOCKS | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &segment_size,
			.boot_val = RELSEG_SIZE,
			.min = RELSEG_SIZE,
			.max = RELSEG_SIZE,
		},
	},

	{
		.name = "send_abort_for_crash",
		.context = PGC_SIGHUP,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "send_abort_for_crash" short description */
		.short_desc = gettext_noop("Send SIGABRT not SIGQUIT to child processes after backend crash."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &send_abort_for_crash,
			.boot_val = false,
		},
	},

	{
		.name = "send_abort_for_kill",
		.context = PGC_SIGHUP,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "send_abort_for_kill" short description */
		.short_desc = gettext_noop("Send SIGABRT not SIGKILL to stuck child processes."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &send_abort_for_kill,
			.boot_val = false,
		},
	},

	{
		.name = "seq_page_cost",
		.context = PGC_USERSET,
		.group = QUERY_TUNING_COST,
		/* translator: GUC parameter "seq_page_cost" short description */
		.short_desc = gettext_noop("Sets the planner's estimate of the cost of a sequentially fetched disk page."),
		.flags = GUC_EXPLAIN,
		.vartype = PGC_REAL,
		._real = {
			.variable = &seq_page_cost,
			.boot_val = DEFAULT_SEQ_PAGE_COST,
			.min = 0,
			.max = DBL_MAX,
		},
	},

	{
		.name = "serializable_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "serializable_buffers" short description */
		.short_desc = gettext_noop("Sets the size of the dedicated buffer pool used for the serializable transaction cache."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &serializable_buffers,
			.boot_val = 32,
			.min = 16,
			.max = SLRU_MAX_ALLOWED_BUFFERS,
			.check_hook = check_serial_buffers,
		},
	},

	{
		.name = "server_encoding",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "server_encoding" short description */
		.short_desc = gettext_noop("Shows the server (database) character set encoding."),
		.flags = GUC_IS_NAME | GUC_REPORT | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &server_encoding_string,
			.boot_val = "SQL_ASCII",
		},
	},

	{
		.name = "server_version",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "server_version" short description */
		.short_desc = gettext_noop("Shows the server version."),
		.flags = GUC_REPORT | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &server_version_string,
			.boot_val = PG_VERSION,
		},
	},

	{
		.name = "server_version_num",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "server_version_num" short description */
		.short_desc = gettext_noop("Shows the server version as an integer."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &server_version_num,
			.boot_val = PG_VERSION_NUM,
			.min = PG_VERSION_NUM,
			.max = PG_VERSION_NUM,
		},
	},

	{
		.name = "session_authorization",
		.context = PGC_USERSET,
		.group = UNGROUPED,
		/* translator: GUC parameter "session_authorization" short description */
		.short_desc = gettext_noop("Sets the session user name."),
		.flags = GUC_IS_NAME | GUC_REPORT | GUC_NO_SHOW_ALL | GUC_NO_RESET_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_NOT_WHILE_SEC_REST,
		.vartype = PGC_STRING,
		._string = {
			.variable = &session_authorization_string,
			.boot_val = NULL,
			.check_hook = check_session_authorization,
			.assign_hook = assign_session_authorization,
		},
	},

	{
		.name = "session_preload_libraries",
		.context = PGC_SUSET,
		.group = CLIENT_CONN_PRELOAD,
		/* translator: GUC parameter "session_preload_libraries" short description */
		.short_desc = gettext_noop("Lists shared libraries to preload into each backend."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE | GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &session_preload_libraries_string,
			.boot_val = "",
		},
	},

	{
		.name = "session_replication_role",
		.context = PGC_SUSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "session_replication_role" short description */
		.short_desc = gettext_noop("Sets the session's behavior for triggers and rewrite rules."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &SessionReplicationRole,
			.boot_val = SESSION_REPLICATION_ROLE_ORIGIN,
			.options = session_replication_role_options,
			.assign_hook = assign_session_replication_role,
		},
	},

	{
		.name = "shared_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "shared_buffers" short description */
		.short_desc = gettext_noop("Sets the number of shared memory buffers used by the server."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &NBuffers,
			.boot_val = 16384,
			.min = 16,
			.max = INT_MAX / 2,
		},
	},

	{
		.name = "shared_memory_size",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "shared_memory_size" short description */
		.short_desc = gettext_noop("Shows the size of the server's main shared memory area (rounded up to the nearest MB)."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_UNIT_MB | GUC_RUNTIME_COMPUTED,
		.vartype = PGC_INT,
		._int = {
			.variable = &shared_memory_size_mb,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "shared_memory_size_in_huge_pages",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "shared_memory_size_in_huge_pages" short description */
		.short_desc = gettext_noop("Shows the number of huge pages needed for the main shared memory area."),
		/* translator: GUC parameter "shared_memory_size_in_huge_pages" long description */
		.long_desc = gettext_noop("-1 means huge pages are not supported."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_RUNTIME_COMPUTED,
		.vartype = PGC_INT,
		._int = {
			.variable = &shared_memory_size_in_huge_pages,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "shared_memory_type",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "shared_memory_type" short description */
		.short_desc = gettext_noop("Selects the shared memory implementation used for the main shared memory region."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &shared_memory_type,
			.boot_val = DEFAULT_SHARED_MEMORY_TYPE,
			.options = shared_memory_options,
		},
	},

	{
		.name = "shared_preload_libraries",
		.context = PGC_POSTMASTER,
		.group = CLIENT_CONN_PRELOAD,
		/* translator: GUC parameter "shared_preload_libraries" short description */
		.short_desc = gettext_noop("Lists shared libraries to preload into server."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE | GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &shared_preload_libraries_string,
			.boot_val = "",
		},
	},

	{
		.name = "ssl",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl" short description */
		.short_desc = gettext_noop("Enables SSL connections."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &EnableSSL,
			.boot_val = false,
			.check_hook = check_ssl,
		},
	},

	{
		.name = "ssl_ca_file",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_ca_file" short description */
		.short_desc = gettext_noop("Location of the SSL certificate authority file."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_ca_file,
			.boot_val = "",
		},
	},

	{
		.name = "ssl_cert_file",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_cert_file" short description */
		.short_desc = gettext_noop("Location of the SSL server certificate file."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_cert_file,
			.boot_val = "server.crt",
		},
	},

	{
		.name = "ssl_ciphers",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_ciphers" short description */
		.short_desc = gettext_noop("Sets the list of allowed TLSv1.2 (and lower) ciphers."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &SSLCipherList,
			.boot_val = DEFAULT_SSL_CIPHERS,
		},
	},

	{
		.name = "ssl_crl_dir",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_crl_dir" short description */
		.short_desc = gettext_noop("Location of the SSL certificate revocation list directory."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_crl_dir,
			.boot_val = "",
		},
	},

	{
		.name = "ssl_crl_file",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_crl_file" short description */
		.short_desc = gettext_noop("Location of the SSL certificate revocation list file."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_crl_file,
			.boot_val = "",
		},
	},

	{
		.name = "ssl_dh_params_file",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_dh_params_file" short description */
		.short_desc = gettext_noop("Location of the SSL DH parameters file."),
		/* translator: GUC parameter "ssl_dh_params_file" long description */
		.long_desc = gettext_noop("An empty string means use compiled-in default parameters."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_dh_params_file,
			.boot_val = "",
		},
	},

	{
		.name = "ssl_groups",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_groups" short description */
		.short_desc = gettext_noop("Sets the group(s) to use for Diffie-Hellman key exchange."),
		/* translator: GUC parameter "ssl_groups" long description */
		.long_desc = gettext_noop("Multiple groups can be specified using a colon-separated list."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &SSLECDHCurve,
			.boot_val = DEFAULT_SSL_GROUPS,
		},
	},

	{
		.name = "ssl_key_file",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_key_file" short description */
		.short_desc = gettext_noop("Location of the SSL server private key file."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_key_file,
			.boot_val = "server.key",
		},
	},

	{
		.name = "ssl_library",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "ssl_library" short description */
		.short_desc = gettext_noop("Shows the name of the SSL library."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_library,
			.boot_val = SSL_LIBRARY,
		},
	},

	{
		.name = "ssl_max_protocol_version",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_max_protocol_version" short description */
		.short_desc = gettext_noop("Sets the maximum SSL/TLS protocol version to use."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &ssl_max_protocol_version,
			.boot_val = PG_TLS_ANY,
			.options = ssl_protocol_versions_info,
		},
	},

	{
		.name = "ssl_min_protocol_version",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_min_protocol_version" short description */
		.short_desc = gettext_noop("Sets the minimum SSL/TLS protocol version to use."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &ssl_min_protocol_version,
			.boot_val = PG_TLS1_2_VERSION,
			.options = ssl_protocol_versions_info + 1,
		},
	},

	{
		.name = "ssl_passphrase_command",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_passphrase_command" short description */
		.short_desc = gettext_noop("Command to obtain passphrases for SSL."),
		/* translator: GUC parameter "ssl_passphrase_command" long description */
		.long_desc = gettext_noop("An empty string means use the built-in prompting mechanism."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &ssl_passphrase_command,
			.boot_val = "",
		},
	},

	{
		.name = "ssl_passphrase_command_supports_reload",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_passphrase_command_supports_reload" short description */
		.short_desc = gettext_noop("Controls whether \"ssl_passphrase_command\" is called during server reload."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &ssl_passphrase_command_supports_reload,
			.boot_val = false,
		},
	},

	{
		.name = "ssl_prefer_server_ciphers",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_prefer_server_ciphers" short description */
		.short_desc = gettext_noop("Give priority to server ciphersuite order."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &SSLPreferServerCiphers,
			.boot_val = true,
		},
	},

	{
		.name = "ssl_renegotiation_limit",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "ssl_renegotiation_limit" short description */
		.short_desc = gettext_noop("SSL renegotiation is no longer supported; this can only be 0."),
		.flags = GUC_NO_SHOW_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &ssl_renegotiation_limit,
			.boot_val = 0,
			.min = 0,
			.max = 0,
		},
	},

	{
		.name = "ssl_sni",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_sni" short description */
		.short_desc = gettext_noop("Sets whether to interpret SNI extensions in SSL connections."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &ssl_sni,
			.boot_val = false,
			.check_hook = check_ssl_sni,
		},
	},

	{
		.name = "ssl_tls13_ciphers",
		.context = PGC_SIGHUP,
		.group = CONN_AUTH_SSL,
		/* translator: GUC parameter "ssl_tls13_ciphers" short description */
		.short_desc = gettext_noop("Sets the list of allowed TLSv1.3 cipher suites."),
		/* translator: GUC parameter "ssl_tls13_ciphers" long description */
		.long_desc = gettext_noop("An empty string means use the default cipher suites."),
		.flags = GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &SSLCipherSuites,
			.boot_val = "",
		},
	},

	{
		.name = "standard_conforming_strings",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "standard_conforming_strings" short description */
		.short_desc = gettext_noop("Nonstandard strings are no longer supported; this can only be true."),
		.flags = GUC_REPORT | GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &standard_conforming_strings,
			.boot_val = true,
			.check_hook = check_standard_conforming_strings,
		},
	},

	{
		.name = "statement_timeout",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "statement_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum allowed duration of any statement."),
		/* translator: GUC parameter "statement_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &StatementTimeout,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "stats_fetch_consistency",
		.context = PGC_USERSET,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "stats_fetch_consistency" short description */
		.short_desc = gettext_noop("Sets the consistency of accesses to statistics data."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &pgstat_fetch_consistency,
			.boot_val = PGSTAT_FETCH_CONSISTENCY_CACHE,
			.options = stats_fetch_consistency,
			.assign_hook = assign_stats_fetch_consistency,
		},
	},

	{
		.name = "subtransaction_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "subtransaction_buffers" short description */
		.short_desc = gettext_noop("Sets the size of the dedicated buffer pool used for the subtransaction cache."),
		/* translator: GUC parameter "subtransaction_buffers" long description */
		.long_desc = gettext_noop("0 means use a fraction of \"shared_buffers\"."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &subtransaction_buffers,
			.boot_val = 0,
			.min = 0,
			.max = SLRU_MAX_ALLOWED_BUFFERS,
			.check_hook = check_subtrans_buffers,
		},
	},

	{
		.name = "summarize_wal",
		.context = PGC_SIGHUP,
		.group = WAL_SUMMARIZATION,
		/* translator: GUC parameter "summarize_wal" short description */
		.short_desc = gettext_noop("Starts the WAL summarizer process to enable incremental backup."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &summarize_wal,
			.boot_val = false,
		},
	},

	{
		.name = "superuser_reserved_connections",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "superuser_reserved_connections" short description */
		.short_desc = gettext_noop("Sets the number of connection slots reserved for superusers."),
		.vartype = PGC_INT,
		._int = {
			.variable = &SuperuserReservedConnections,
			.boot_val = 3,
			.min = 0,
			.max = MAX_BACKENDS,
		},
	},

	{
		.name = "sync_replication_slots",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "sync_replication_slots" short description */
		.short_desc = gettext_noop("Enables a physical standby to synchronize logical failover replication slots from the primary server."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &sync_replication_slots,
			.boot_val = false,
		},
	},

	{
		.name = "synchronize_seqscans",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_PREVIOUS,
		/* translator: GUC parameter "synchronize_seqscans" short description */
		.short_desc = gettext_noop("Enables synchronized sequential scans."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &synchronize_seqscans,
			.boot_val = true,
		},
	},

	{
		.name = "synchronized_standby_slots",
		.context = PGC_SIGHUP,
		.group = REPLICATION_PRIMARY,
		/* translator: GUC parameter "synchronized_standby_slots" short description */
		.short_desc = gettext_noop("Lists streaming replication standby server replication slot names that logical WAL sender processes will wait for."),
		/* translator: GUC parameter "synchronized_standby_slots" long description */
		.long_desc = gettext_noop("Logical WAL sender processes will send decoded changes to output plugins only after the specified replication slots have confirmed receiving WAL."),
		.flags = GUC_LIST_INPUT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &synchronized_standby_slots,
			.boot_val = "",
			.check_hook = check_synchronized_standby_slots,
			.assign_hook = assign_synchronized_standby_slots,
		},
	},

	{
		.name = "synchronous_commit",
		.context = PGC_USERSET,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "synchronous_commit" short description */
		.short_desc = gettext_noop("Sets the current transaction's synchronization level."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &synchronous_commit,
			.boot_val = SYNCHRONOUS_COMMIT_ON,
			.options = synchronous_commit_options,
			.assign_hook = assign_synchronous_commit,
		},
	},

	{
		.name = "synchronous_standby_names",
		.context = PGC_SIGHUP,
		.group = REPLICATION_PRIMARY,
		/* translator: GUC parameter "synchronous_standby_names" short description */
		.short_desc = gettext_noop("Number of synchronous standbys and list of names of potential synchronous ones."),
		.flags = GUC_LIST_INPUT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &SyncRepStandbyNames,
			.boot_val = "",
			.check_hook = check_synchronous_standby_names,
			.assign_hook = assign_synchronous_standby_names,
		},
	},

	{
		.name = "syslog_facility",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "syslog_facility" short description */
		.short_desc = gettext_noop("Sets the syslog \"facility\" to be used when syslog enabled."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &syslog_facility,
			.boot_val = DEFAULT_SYSLOG_FACILITY,
			.options = syslog_facility_options,
			.assign_hook = assign_syslog_facility,
		},
	},

	{
		.name = "syslog_ident",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "syslog_ident" short description */
		.short_desc = gettext_noop("Sets the program name used to identify PostgreSQL messages in syslog."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &syslog_ident_str,
			.boot_val = "postgres",
			.assign_hook = assign_syslog_ident,
		},
	},

	{
		.name = "syslog_sequence_numbers",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "syslog_sequence_numbers" short description */
		.short_desc = gettext_noop("Add sequence number to syslog messages to avoid duplicate suppression."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &syslog_sequence_numbers,
			.boot_val = true,
		},
	},

	{
		.name = "syslog_split_messages",
		.context = PGC_SIGHUP,
		.group = LOGGING_WHERE,
		/* translator: GUC parameter "syslog_split_messages" short description */
		.short_desc = gettext_noop("Split messages sent to syslog by lines and to fit into 1024 bytes."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &syslog_split_messages,
			.boot_val = true,
		},
	},

	{
		.name = "tcp_keepalives_count",
		.context = PGC_USERSET,
		.group = CONN_AUTH_TCP,
		/* translator: GUC parameter "tcp_keepalives_count" short description */
		.short_desc = gettext_noop("Maximum number of TCP keepalive retransmits."),
		/* translator: GUC parameter "tcp_keepalives_count" long description */
		.long_desc = gettext_noop("Number of consecutive keepalive retransmits that can be lost before a connection is considered dead. 0 means use the system default."),
		.vartype = PGC_INT,
		._int = {
			.variable = &tcp_keepalives_count,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
			.assign_hook = assign_tcp_keepalives_count,
			.show_hook = show_tcp_keepalives_count,
		},
	},

	{
		.name = "tcp_keepalives_idle",
		.context = PGC_USERSET,
		.group = CONN_AUTH_TCP,
		/* translator: GUC parameter "tcp_keepalives_idle" short description */
		.short_desc = gettext_noop("Time between issuing TCP keepalives."),
		/* translator: GUC parameter "tcp_keepalives_idle" long description */
		.long_desc = gettext_noop("0 means use the system default."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &tcp_keepalives_idle,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
			.assign_hook = assign_tcp_keepalives_idle,
			.show_hook = show_tcp_keepalives_idle,
		},
	},

	{
		.name = "tcp_keepalives_interval",
		.context = PGC_USERSET,
		.group = CONN_AUTH_TCP,
		/* translator: GUC parameter "tcp_keepalives_interval" short description */
		.short_desc = gettext_noop("Time between TCP keepalive retransmits."),
		/* translator: GUC parameter "tcp_keepalives_interval" long description */
		.long_desc = gettext_noop("0 means use the system default."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &tcp_keepalives_interval,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
			.assign_hook = assign_tcp_keepalives_interval,
			.show_hook = show_tcp_keepalives_interval,
		},
	},

	{
		.name = "tcp_user_timeout",
		.context = PGC_USERSET,
		.group = CONN_AUTH_TCP,
		/* translator: GUC parameter "tcp_user_timeout" short description */
		.short_desc = gettext_noop("TCP user timeout."),
		/* translator: GUC parameter "tcp_user_timeout" long description */
		.long_desc = gettext_noop("0 means use the system default."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &tcp_user_timeout,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
			.assign_hook = assign_tcp_user_timeout,
			.show_hook = show_tcp_user_timeout,
		},
	},

	{
		.name = "temp_buffers",
		.context = PGC_USERSET,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "temp_buffers" short description */
		.short_desc = gettext_noop("Sets the maximum number of temporary buffers used by each session."),
		.flags = GUC_UNIT_BLOCKS | GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &num_temp_buffers,
			.boot_val = 1024,
			.min = 100,
			.max = INT_MAX / 2,
			.check_hook = check_temp_buffers,
		},
	},

	{
		.name = "temp_file_limit",
		.context = PGC_SUSET,
		.group = RESOURCES_DISK,
		/* translator: GUC parameter "temp_file_limit" short description */
		.short_desc = gettext_noop("Limits the total size of all temporary files used by each process."),
		/* translator: GUC parameter "temp_file_limit" long description */
		.long_desc = gettext_noop("-1 means no limit."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &temp_file_limit,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "temp_tablespaces",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "temp_tablespaces" short description */
		.short_desc = gettext_noop("Sets the tablespace(s) to use for temporary tables and sort files."),
		/* translator: GUC parameter "temp_tablespaces" long description */
		.long_desc = gettext_noop("An empty string means use the database's default tablespace."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &temp_tablespaces,
			.boot_val = "",
			.check_hook = check_temp_tablespaces,
			.assign_hook = assign_temp_tablespaces,
		},
	},

	{
		.name = "TimeZone",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "TimeZone" short description */
		.short_desc = gettext_noop("Sets the time zone for displaying and interpreting time stamps."),
		.flags = GUC_REPORT,
		.vartype = PGC_STRING,
		._string = {
			.variable = &timezone_string,
			.boot_val = "GMT",
			.check_hook = check_timezone,
			.assign_hook = assign_timezone,
			.show_hook = show_timezone,
		},
	},

	{
		.name = "timezone_abbreviations",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_LOCALE,
		/* translator: GUC parameter "timezone_abbreviations" short description */
		.short_desc = gettext_noop("Selects a file of time zone abbreviations."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &timezone_abbreviations_string,
			.boot_val = NULL,
			.check_hook = check_timezone_abbreviations,
			.assign_hook = assign_timezone_abbreviations,
		},
	},

	{
		.name = "timing_clock_source",
		.context = PGC_SUSET,
		.group = RESOURCES_TIME,
		/* translator: GUC parameter "timing_clock_source" short description */
		.short_desc = gettext_noop("Controls the clock source used for collecting timing measurements."),
		/* translator: GUC parameter "timing_clock_source" long description */
		.long_desc = gettext_noop("This enables the use of specialized clock sources, specifically the RDTSC clock source on x86-64 systems (if available), to support timing measurements with lower overhead during EXPLAIN and other instrumentation."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &timing_clock_source,
			.boot_val = TIMING_CLOCK_SOURCE_AUTO,
			.options = timing_clock_source_options,
			.check_hook = check_timing_clock_source,
			.assign_hook = assign_timing_clock_source,
			.show_hook = show_timing_clock_source,
		},
	},

	{
		.name = "trace_connection_negotiation",
		.context = PGC_POSTMASTER,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_connection_negotiation" short description */
		.short_desc = gettext_noop("Logs details of pre-authentication connection handshake."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Trace_connection_negotiation,
			.boot_val = false,
		},
	},

#ifdef LOCK_DEBUG
	{
		.name = "trace_lock_oidmin",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_lock_oidmin" short description */
		.short_desc = gettext_noop("Sets the minimum OID of tables for tracking locks."),
		/* translator: GUC parameter "trace_lock_oidmin" long description */
		.long_desc = gettext_noop("Is used to avoid output on system tables."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_INT,
		._int = {
			.variable = &Trace_lock_oidmin,
			.boot_val = FirstNormalObjectId,
			.min = 0,
			.max = INT_MAX,
		},
	},
#endif

#ifdef LOCK_DEBUG
	{
		.name = "trace_lock_table",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_lock_table" short description */
		.short_desc = gettext_noop("Sets the OID of the table with unconditionally lock tracing."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_INT,
		._int = {
			.variable = &Trace_lock_table,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
		},
	},
#endif

#ifdef LOCK_DEBUG
	{
		.name = "trace_locks",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_locks" short description */
		.short_desc = gettext_noop("Emits information about lock usage."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Trace_locks,
			.boot_val = false,
		},
	},
#endif

#ifdef LOCK_DEBUG
	{
		.name = "trace_lwlocks",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_lwlocks" short description */
		.short_desc = gettext_noop("Emits information about lightweight lock usage."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Trace_lwlocks,
			.boot_val = false,
		},
	},
#endif

	{
		.name = "trace_notify",
		.context = PGC_USERSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_notify" short description */
		.short_desc = gettext_noop("Generates debugging output for LISTEN and NOTIFY."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Trace_notify,
			.boot_val = false,
		},
	},

	{
		.name = "trace_sort",
		.context = PGC_USERSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_sort" short description */
		.short_desc = gettext_noop("Emit information about resource usage in sorting."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &trace_sort,
			.boot_val = false,
		},
	},

#ifdef TRACE_SYNCSCAN
	{
		.name = "trace_syncscan",
		.context = PGC_USERSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_syncscan" short description */
		.short_desc = gettext_noop("Generate debugging output for synchronized scanning."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &trace_syncscan,
			.boot_val = false,
		},
	},
#endif

#ifdef LOCK_DEBUG
	{
		.name = "trace_userlocks",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "trace_userlocks" short description */
		.short_desc = gettext_noop("Emits information about user lock usage."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Trace_userlocks,
			.boot_val = false,
		},
	},
#endif

	{
		.name = "track_activities",
		.context = PGC_SUSET,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "track_activities" short description */
		.short_desc = gettext_noop("Collects information about executing commands."),
		/* translator: GUC parameter "track_activities" long description */
		.long_desc = gettext_noop("Enables the collection of information on the currently executing command of each session, along with the time at which that command began execution."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &pgstat_track_activities,
			.boot_val = true,
		},
	},

	{
		.name = "track_activity_query_size",
		.context = PGC_POSTMASTER,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "track_activity_query_size" short description */
		.short_desc = gettext_noop("Sets the size reserved for pg_stat_activity.query, in bytes."),
		.flags = GUC_UNIT_BYTE,
		.vartype = PGC_INT,
		._int = {
			.variable = &pgstat_track_activity_query_size,
			.boot_val = 1024,
			.min = 100,
			.max = 1048576,
		},
	},

	{
		.name = "track_commit_timestamp",
		.context = PGC_POSTMASTER,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "track_commit_timestamp" short description */
		.short_desc = gettext_noop("Collects transaction commit time."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &track_commit_timestamp,
			.boot_val = false,
		},
	},

	{
		.name = "track_cost_delay_timing",
		.context = PGC_SUSET,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "track_cost_delay_timing" short description */
		.short_desc = gettext_noop("Collects timing statistics for cost-based vacuum delay."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &track_cost_delay_timing,
			.boot_val = false,
		},
	},

	{
		.name = "track_counts",
		.context = PGC_SUSET,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "track_counts" short description */
		.short_desc = gettext_noop("Collects statistics on database activity."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &pgstat_track_counts,
			.boot_val = true,
		},
	},

	{
		.name = "track_functions",
		.context = PGC_SUSET,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "track_functions" short description */
		.short_desc = gettext_noop("Collects function-level statistics on database activity."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &pgstat_track_functions,
			.boot_val = TRACK_FUNC_OFF,
			.options = track_function_options,
		},
	},

	{
		.name = "track_io_timing",
		.context = PGC_SUSET,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "track_io_timing" short description */
		.short_desc = gettext_noop("Collects timing statistics for database I/O activity."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &track_io_timing,
			.boot_val = false,
		},
	},

	{
		.name = "track_wal_io_timing",
		.context = PGC_SUSET,
		.group = STATS_CUMULATIVE,
		/* translator: GUC parameter "track_wal_io_timing" short description */
		.short_desc = gettext_noop("Collects timing statistics for WAL I/O activity."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &track_wal_io_timing,
			.boot_val = false,
		},
	},

	{
		.name = "transaction_buffers",
		.context = PGC_POSTMASTER,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "transaction_buffers" short description */
		.short_desc = gettext_noop("Sets the size of the dedicated buffer pool used for the transaction status cache."),
		/* translator: GUC parameter "transaction_buffers" long description */
		.long_desc = gettext_noop("0 means use a fraction of \"shared_buffers\"."),
		.flags = GUC_UNIT_BLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &transaction_buffers,
			.boot_val = 0,
			.min = 0,
			.max = SLRU_MAX_ALLOWED_BUFFERS,
			.check_hook = check_transaction_buffers,
		},
	},

	{
		.name = "transaction_deferrable",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "transaction_deferrable" short description */
		.short_desc = gettext_noop("Whether to defer a read-only serializable transaction until it can be executed with no possible serialization failures."),
		.flags = GUC_NO_RESET | GUC_NO_RESET_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &XactDeferrable,
			.boot_val = false,
			.check_hook = check_transaction_deferrable,
		},
	},

	{
		.name = "transaction_isolation",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "transaction_isolation" short description */
		.short_desc = gettext_noop("Sets the current transaction's isolation level."),
		.flags = GUC_NO_RESET | GUC_NO_RESET_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &XactIsoLevel,
			.boot_val = XACT_READ_COMMITTED,
			.options = isolation_level_options,
			.check_hook = check_transaction_isolation,
		},
	},

	{
		.name = "transaction_read_only",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "transaction_read_only" short description */
		.short_desc = gettext_noop("Sets the current transaction's read-only status."),
		.flags = GUC_NO_RESET | GUC_NO_RESET_ALL | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &XactReadOnly,
			.boot_val = false,
			.check_hook = check_transaction_read_only,
		},
	},

	{
		.name = "transaction_timeout",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "transaction_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum allowed duration of any transaction within a session (not a prepared transaction)."),
		/* translator: GUC parameter "transaction_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &TransactionTimeout,
			.boot_val = 0,
			.min = 0,
			.max = INT_MAX,
			.assign_hook = assign_transaction_timeout,
		},
	},

	{
		.name = "transform_null_equals",
		.context = PGC_USERSET,
		.group = COMPAT_OPTIONS_OTHER,
		/* translator: GUC parameter "transform_null_equals" short description */
		.short_desc = gettext_noop("Treats \"expr=NULL\" as \"expr IS NULL\"."),
		/* translator: GUC parameter "transform_null_equals" long description */
		.long_desc = gettext_noop("When turned on, expressions of the form expr = NULL (or NULL = expr) are treated as expr IS NULL, that is, they return true if expr evaluates to the null value, and false otherwise. The correct behavior of expr = NULL is to always return null (unknown)."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &Transform_null_equals,
			.boot_val = false,
		},
	},

	{
		.name = "unix_socket_directories",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "unix_socket_directories" short description */
		.short_desc = gettext_noop("Sets the directories where Unix-domain sockets will be created."),
		.flags = GUC_LIST_INPUT | GUC_LIST_QUOTE | GUC_SUPERUSER_ONLY,
		.vartype = PGC_STRING,
		._string = {
			.variable = &Unix_socket_directories,
			.boot_val = DEFAULT_PGSOCKET_DIR,
		},
	},

	{
		.name = "unix_socket_group",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "unix_socket_group" short description */
		.short_desc = gettext_noop("Sets the owning group of the Unix-domain socket."),
		/* translator: GUC parameter "unix_socket_group" long description */
		.long_desc = gettext_noop("The owning user of the socket is always the user that starts the server. An empty string means use the user's default group."),
		.vartype = PGC_STRING,
		._string = {
			.variable = &Unix_socket_group,
			.boot_val = "",
		},
	},

	{
		.name = "unix_socket_permissions",
		.context = PGC_POSTMASTER,
		.group = CONN_AUTH_SETTINGS,
		/* translator: GUC parameter "unix_socket_permissions" short description */
		.short_desc = gettext_noop("Sets the access permissions of the Unix-domain socket."),
		/* translator: GUC parameter "unix_socket_permissions" long description */
		.long_desc = gettext_noop("Unix-domain sockets use the usual Unix file system permission set. The parameter value is expected to be a numeric mode specification in the form accepted by the chmod and umask system calls. (To use the customary octal format the number must start with a 0 (zero).)"),
		.flags = GUC_SHOW_IN_OCTAL,
		.vartype = PGC_INT,
		._int = {
			.variable = &Unix_socket_permissions,
			.boot_val = 0777,
			.min = 0000,
			.max = 0777,
		},
	},

	{
		.name = "update_process_title",
		.context = PGC_SUSET,
		.group = PROCESS_TITLE,
		/* translator: GUC parameter "update_process_title" short description */
		.short_desc = gettext_noop("Updates the process title to show the active SQL command."),
		/* translator: GUC parameter "update_process_title" long description */
		.long_desc = gettext_noop("Enables updating of the process title every time a new SQL command is received by the server."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &update_process_title,
			.boot_val = DEFAULT_UPDATE_PROCESS_TITLE,
		},
	},

	{
		.name = "vacuum_buffer_usage_limit",
		.context = PGC_USERSET,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "vacuum_buffer_usage_limit" short description */
		.short_desc = gettext_noop("Sets the buffer pool size for VACUUM, ANALYZE, and autovacuum."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &VacuumBufferUsageLimit,
			.boot_val = 2048,
			.min = 0,
			.max = MAX_BAS_VAC_RING_SIZE_KB,
			.check_hook = check_vacuum_buffer_usage_limit,
		},
	},

	{
		.name = "vacuum_cost_delay",
		.context = PGC_USERSET,
		.group = VACUUM_COST_DELAY,
		/* translator: GUC parameter "vacuum_cost_delay" short description */
		.short_desc = gettext_noop("Vacuum cost delay in milliseconds."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_REAL,
		._real = {
			.variable = &VacuumCostDelay,
			.boot_val = 0,
			.min = 0,
			.max = 100,
		},
	},

	{
		.name = "vacuum_cost_limit",
		.context = PGC_USERSET,
		.group = VACUUM_COST_DELAY,
		/* translator: GUC parameter "vacuum_cost_limit" short description */
		.short_desc = gettext_noop("Vacuum cost amount available before napping."),
		.vartype = PGC_INT,
		._int = {
			.variable = &VacuumCostLimit,
			.boot_val = 200,
			.min = 1,
			.max = 10000,
		},
	},

	{
		.name = "vacuum_cost_page_dirty",
		.context = PGC_USERSET,
		.group = VACUUM_COST_DELAY,
		/* translator: GUC parameter "vacuum_cost_page_dirty" short description */
		.short_desc = gettext_noop("Vacuum cost for a page dirtied by vacuum."),
		.vartype = PGC_INT,
		._int = {
			.variable = &VacuumCostPageDirty,
			.boot_val = 20,
			.min = 0,
			.max = 10000,
		},
	},

	{
		.name = "vacuum_cost_page_hit",
		.context = PGC_USERSET,
		.group = VACUUM_COST_DELAY,
		/* translator: GUC parameter "vacuum_cost_page_hit" short description */
		.short_desc = gettext_noop("Vacuum cost for a page found in the buffer cache."),
		.vartype = PGC_INT,
		._int = {
			.variable = &VacuumCostPageHit,
			.boot_val = 1,
			.min = 0,
			.max = 10000,
		},
	},

	{
		.name = "vacuum_cost_page_miss",
		.context = PGC_USERSET,
		.group = VACUUM_COST_DELAY,
		/* translator: GUC parameter "vacuum_cost_page_miss" short description */
		.short_desc = gettext_noop("Vacuum cost for a page not found in the buffer cache."),
		.vartype = PGC_INT,
		._int = {
			.variable = &VacuumCostPageMiss,
			.boot_val = 2,
			.min = 0,
			.max = 10000,
		},
	},

	{
		.name = "vacuum_failsafe_age",
		.context = PGC_USERSET,
		.group = VACUUM_FREEZING,
		/* translator: GUC parameter "vacuum_failsafe_age" short description */
		.short_desc = gettext_noop("Age at which VACUUM should trigger failsafe to avoid a wraparound outage."),
		.vartype = PGC_INT,
		._int = {
			.variable = &vacuum_failsafe_age,
			.boot_val = 1600000000,
			.min = 0,
			.max = 2100000000,
		},
	},

	{
		.name = "vacuum_freeze_min_age",
		.context = PGC_USERSET,
		.group = VACUUM_FREEZING,
		/* translator: GUC parameter "vacuum_freeze_min_age" short description */
		.short_desc = gettext_noop("Minimum age at which VACUUM should freeze a table row."),
		.vartype = PGC_INT,
		._int = {
			.variable = &vacuum_freeze_min_age,
			.boot_val = 50000000,
			.min = 0,
			.max = 1000000000,
		},
	},

	{
		.name = "vacuum_freeze_table_age",
		.context = PGC_USERSET,
		.group = VACUUM_FREEZING,
		/* translator: GUC parameter "vacuum_freeze_table_age" short description */
		.short_desc = gettext_noop("Age at which VACUUM should scan whole table to freeze tuples."),
		.vartype = PGC_INT,
		._int = {
			.variable = &vacuum_freeze_table_age,
			.boot_val = 150000000,
			.min = 0,
			.max = 2000000000,
		},
	},

	{
		.name = "vacuum_max_eager_freeze_failure_rate",
		.context = PGC_USERSET,
		.group = VACUUM_FREEZING,
		/* translator: GUC parameter "vacuum_max_eager_freeze_failure_rate" short description */
		.short_desc = gettext_noop("Fraction of pages in a relation vacuum can scan and fail to freeze before disabling eager scanning."),
		/* translator: GUC parameter "vacuum_max_eager_freeze_failure_rate" long description */
		.long_desc = gettext_noop("A value of 0.0 disables eager scanning and a value of 1.0 will eagerly scan up to 100 percent of the all-visible pages in the relation. If vacuum successfully freezes these pages, the cap is lower than 100 percent, because the goal is to amortize page freezing across multiple vacuums."),
		.vartype = PGC_REAL,
		._real = {
			.variable = &vacuum_max_eager_freeze_failure_rate,
			.boot_val = 0.03,
			.min = 0.0,
			.max = 1.0,
		},
	},

	{
		.name = "vacuum_multixact_failsafe_age",
		.context = PGC_USERSET,
		.group = VACUUM_FREEZING,
		/* translator: GUC parameter "vacuum_multixact_failsafe_age" short description */
		.short_desc = gettext_noop("Multixact age at which VACUUM should trigger failsafe to avoid a wraparound outage."),
		.vartype = PGC_INT,
		._int = {
			.variable = &vacuum_multixact_failsafe_age,
			.boot_val = 1600000000,
			.min = 0,
			.max = 2100000000,
		},
	},

	{
		.name = "vacuum_multixact_freeze_min_age",
		.context = PGC_USERSET,
		.group = VACUUM_FREEZING,
		/* translator: GUC parameter "vacuum_multixact_freeze_min_age" short description */
		.short_desc = gettext_noop("Minimum age at which VACUUM should freeze a MultiXactId in a table row."),
		.vartype = PGC_INT,
		._int = {
			.variable = &vacuum_multixact_freeze_min_age,
			.boot_val = 5000000,
			.min = 0,
			.max = 1000000000,
		},
	},

	{
		.name = "vacuum_multixact_freeze_table_age",
		.context = PGC_USERSET,
		.group = VACUUM_FREEZING,
		/* translator: GUC parameter "vacuum_multixact_freeze_table_age" short description */
		.short_desc = gettext_noop("Multixact age at which VACUUM should scan whole table to freeze tuples."),
		.vartype = PGC_INT,
		._int = {
			.variable = &vacuum_multixact_freeze_table_age,
			.boot_val = 150000000,
			.min = 0,
			.max = 2000000000,
		},
	},

	{
		.name = "vacuum_truncate",
		.context = PGC_USERSET,
		.group = VACUUM_DEFAULT,
		/* translator: GUC parameter "vacuum_truncate" short description */
		.short_desc = gettext_noop("Enables vacuum to truncate empty pages at the end of the table."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &vacuum_truncate,
			.boot_val = true,
		},
	},

	{
		.name = "wal_block_size",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "wal_block_size" short description */
		.short_desc = gettext_noop("Shows the block size in the write ahead log."),
		.flags = GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_block_size,
			.boot_val = XLOG_BLCKSZ,
			.min = XLOG_BLCKSZ,
			.max = XLOG_BLCKSZ,
		},
	},

	{
		.name = "wal_buffers",
		.context = PGC_POSTMASTER,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_buffers" short description */
		.short_desc = gettext_noop("Sets the number of disk-page buffers in shared memory for WAL."),
		/* translator: GUC parameter "wal_buffers" long description */
		.long_desc = gettext_noop("-1 means use a fraction of \"shared_buffers\"."),
		.flags = GUC_UNIT_XBLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &XLOGbuffers,
			.boot_val = -1,
			.min = -1,
			.max = (INT_MAX / XLOG_BLCKSZ),
			.check_hook = check_wal_buffers,
		},
	},

	{
		.name = "wal_compression",
		.context = PGC_SUSET,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_compression" short description */
		.short_desc = gettext_noop("Compresses full-page writes written in WAL file with specified method."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &wal_compression,
			.boot_val = WAL_COMPRESSION_NONE,
			.options = wal_compression_options,
		},
	},

	{
		.name = "wal_consistency_checking",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "wal_consistency_checking" short description */
		.short_desc = gettext_noop("Sets the WAL resource managers for which WAL consistency checks are done."),
		/* translator: GUC parameter "wal_consistency_checking" long description */
		.long_desc = gettext_noop("Full-page images will be logged for all data blocks and cross-checked against the results of WAL replay."),
		.flags = GUC_LIST_INPUT | GUC_NOT_IN_SAMPLE,
		.vartype = PGC_STRING,
		._string = {
			.variable = &wal_consistency_checking_string,
			.boot_val = "",
			.check_hook = check_wal_consistency_checking,
			.assign_hook = assign_wal_consistency_checking,
		},
	},

#ifdef WAL_DEBUG
	{
		.name = "wal_debug",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "wal_debug" short description */
		.short_desc = gettext_noop("Emit WAL-related debugging output."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &XLOG_DEBUG,
			.boot_val = false,
		},
	},
#endif

	{
		.name = "wal_decode_buffer_size",
		.context = PGC_POSTMASTER,
		.group = WAL_RECOVERY,
		/* translator: GUC parameter "wal_decode_buffer_size" short description */
		.short_desc = gettext_noop("Buffer size for reading ahead in the WAL during recovery."),
		/* translator: GUC parameter "wal_decode_buffer_size" long description */
		.long_desc = gettext_noop("Maximum distance to read ahead in the WAL to prefetch referenced data blocks."),
		.flags = GUC_UNIT_BYTE,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_decode_buffer_size,
			.boot_val = 512 * 1024,
			.min = 64 * 1024,
			.max = MaxAllocSize,
		},
	},

	{
		.name = "wal_init_zero",
		.context = PGC_SUSET,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_init_zero" short description */
		.short_desc = gettext_noop("Writes zeroes to new WAL files before first use."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &wal_init_zero,
			.boot_val = true,
		},
	},

	{
		.name = "wal_keep_size",
		.context = PGC_SIGHUP,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "wal_keep_size" short description */
		.short_desc = gettext_noop("Sets the size of WAL files held for standby servers."),
		.flags = GUC_UNIT_MB,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_keep_size_mb,
			.boot_val = 0,
			.min = 0,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "wal_level",
		.context = PGC_POSTMASTER,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_level" short description */
		.short_desc = gettext_noop("Sets the level of information written to the WAL."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &wal_level,
			.boot_val = WAL_LEVEL_REPLICA,
			.options = wal_level_options,
		},
	},

	{
		.name = "wal_log_hints",
		.context = PGC_POSTMASTER,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_log_hints" short description */
		.short_desc = gettext_noop("Writes full pages to WAL when first modified after a checkpoint, even for a non-critical modification."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &wal_log_hints,
			.boot_val = false,
		},
	},

	{
		.name = "wal_receiver_create_temp_slot",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "wal_receiver_create_temp_slot" short description */
		.short_desc = gettext_noop("Sets whether a WAL receiver should create a temporary replication slot if no permanent slot is configured."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &wal_receiver_create_temp_slot,
			.boot_val = false,
		},
	},

	{
		.name = "wal_receiver_status_interval",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "wal_receiver_status_interval" short description */
		.short_desc = gettext_noop("Sets the maximum interval between WAL receiver status reports to the sending server."),
		.flags = GUC_UNIT_S,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_receiver_status_interval,
			.boot_val = 10,
			.min = 0,
			.max = INT_MAX / 1000,
		},
	},

	{
		.name = "wal_receiver_timeout",
		.context = PGC_USERSET,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "wal_receiver_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum wait time to receive data from the sending server."),
		/* translator: GUC parameter "wal_receiver_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_receiver_timeout,
			.boot_val = 60 * 1000,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "wal_recycle",
		.context = PGC_SUSET,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_recycle" short description */
		.short_desc = gettext_noop("Recycles WAL files by renaming them."),
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &wal_recycle,
			.boot_val = true,
		},
	},

	{
		.name = "wal_retrieve_retry_interval",
		.context = PGC_SIGHUP,
		.group = REPLICATION_STANDBY,
		/* translator: GUC parameter "wal_retrieve_retry_interval" short description */
		.short_desc = gettext_noop("Sets the time to wait before retrying to retrieve WAL after a failed attempt."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_retrieve_retry_interval,
			.boot_val = 5000,
			.min = 1,
			.max = INT_MAX,
		},
	},

	{
		.name = "wal_segment_size",
		.context = PGC_INTERNAL,
		.group = PRESET_OPTIONS,
		/* translator: GUC parameter "wal_segment_size" short description */
		.short_desc = gettext_noop("Shows the size of write ahead log segments."),
		.flags = GUC_UNIT_BYTE | GUC_NOT_IN_SAMPLE | GUC_DISALLOW_IN_FILE | GUC_RUNTIME_COMPUTED,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_segment_size,
			.boot_val = DEFAULT_XLOG_SEG_SIZE,
			.min = WalSegMinSize,
			.max = WalSegMaxSize,
			.check_hook = check_wal_segment_size,
		},
	},

	{
		.name = "wal_sender_shutdown_timeout",
		.context = PGC_USERSET,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "wal_sender_shutdown_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum time the server waits during shutdown for all WAL data to be replicated to the receiver."),
		/* translator: GUC parameter "wal_sender_shutdown_timeout" long description */
		.long_desc = gettext_noop("-1 means wait indefinitely. 0 means do not wait."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_sender_shutdown_timeout,
			.boot_val = -1,
			.min = -1,
			.max = INT_MAX,
		},
	},

	{
		.name = "wal_sender_timeout",
		.context = PGC_USERSET,
		.group = REPLICATION_SENDING,
		/* translator: GUC parameter "wal_sender_timeout" short description */
		.short_desc = gettext_noop("Sets the maximum time to wait for WAL replication."),
		/* translator: GUC parameter "wal_sender_timeout" long description */
		.long_desc = gettext_noop("0 disables the timeout."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_sender_timeout,
			.boot_val = 60 * 1000,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "wal_skip_threshold",
		.context = PGC_USERSET,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_skip_threshold" short description */
		.short_desc = gettext_noop("Minimum size of new file to fsync instead of writing WAL."),
		.flags = GUC_UNIT_KB,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_skip_threshold,
			.boot_val = 2048,
			.min = 0,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "wal_summary_keep_time",
		.context = PGC_SIGHUP,
		.group = WAL_SUMMARIZATION,
		/* translator: GUC parameter "wal_summary_keep_time" short description */
		.short_desc = gettext_noop("Time for which WAL summary files should be kept."),
		/* translator: GUC parameter "wal_summary_keep_time" long description */
		.long_desc = gettext_noop("0 disables automatic summary file deletion."),
		.flags = GUC_UNIT_MIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &wal_summary_keep_time,
			.boot_val = 10 * HOURS_PER_DAY * MINS_PER_HOUR /* 10 days */,
			.min = 0,
			.max = INT_MAX / SECS_PER_MINUTE,
		},
	},

	{
		.name = "wal_sync_method",
		.context = PGC_SIGHUP,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_sync_method" short description */
		.short_desc = gettext_noop("Selects the method used for forcing WAL updates to disk."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &wal_sync_method,
			.boot_val = DEFAULT_WAL_SYNC_METHOD,
			.options = wal_sync_method_options,
			.assign_hook = assign_wal_sync_method,
		},
	},

	{
		.name = "wal_writer_delay",
		.context = PGC_SIGHUP,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_writer_delay" short description */
		.short_desc = gettext_noop("Time between WAL flushes performed in the WAL writer."),
		.flags = GUC_UNIT_MS,
		.vartype = PGC_INT,
		._int = {
			.variable = &WalWriterDelay,
			.boot_val = 200,
			.min = 1,
			.max = 10000,
		},
	},

	{
		.name = "wal_writer_flush_after",
		.context = PGC_SIGHUP,
		.group = WAL_SETTINGS,
		/* translator: GUC parameter "wal_writer_flush_after" short description */
		.short_desc = gettext_noop("Amount of WAL written out by WAL writer that triggers a flush."),
		.flags = GUC_UNIT_XBLOCKS,
		.vartype = PGC_INT,
		._int = {
			.variable = &WalWriterFlushAfter,
			.boot_val = DEFAULT_WAL_WRITER_FLUSH_AFTER,
			.min = 0,
			.max = INT_MAX,
		},
	},

	{
		.name = "work_mem",
		.context = PGC_USERSET,
		.group = RESOURCES_MEM,
		/* translator: GUC parameter "work_mem" short description */
		.short_desc = gettext_noop("Sets the maximum memory to be used for query workspaces."),
		/* translator: GUC parameter "work_mem" long description */
		.long_desc = gettext_noop("This much memory can be used by each internal sort operation and hash table before switching to temporary disk files."),
		.flags = GUC_UNIT_KB | GUC_EXPLAIN,
		.vartype = PGC_INT,
		._int = {
			.variable = &work_mem,
			.boot_val = 4096,
			.min = 64,
			.max = MAX_KILOBYTES,
		},
	},

	{
		.name = "xmlbinary",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "xmlbinary" short description */
		.short_desc = gettext_noop("Sets how binary values are to be encoded in XML."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &xmlbinary,
			.boot_val = XMLBINARY_BASE64,
			.options = xmlbinary_options,
		},
	},

	{
		.name = "xmloption",
		.context = PGC_USERSET,
		.group = CLIENT_CONN_STATEMENT,
		/* translator: GUC parameter "xmloption" short description */
		.short_desc = gettext_noop("Sets whether XML data in implicit parsing and serialization operations is to be considered as documents or content fragments."),
		.vartype = PGC_ENUM,
		._enum = {
			.variable = &xmloption,
			.boot_val = XMLOPTION_CONTENT,
			.options = xmloption_options,
		},
	},

	{
		.name = "zero_damaged_pages",
		.context = PGC_SUSET,
		.group = DEVELOPER_OPTIONS,
		/* translator: GUC parameter "zero_damaged_pages" short description */
		.short_desc = gettext_noop("Continues processing past damaged page headers."),
		/* translator: GUC parameter "zero_damaged_pages" long description */
		.long_desc = gettext_noop("Detection of a damaged page header normally causes PostgreSQL to report an error, aborting the current transaction. Setting \"zero_damaged_pages\" to true causes the system to instead report a warning, zero out the damaged page, and continue processing. This behavior will destroy data, namely all the rows on the damaged page."),
		.flags = GUC_NOT_IN_SAMPLE,
		.vartype = PGC_BOOL,
		._bool = {
			.variable = &zero_damaged_pages,
			.boot_val = false,
		},
	},

	/* End-of-list marker */
	{0}
};
